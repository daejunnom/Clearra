[package]
name = "clearra-desktop"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
clearra-gui-host = { path = "../../../crates/clearra-gui-host", features = ["native-c-core", "webgpu-search"] }
serde_json = "=1.0.145"
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }

[lints.rust]
unsafe_code = "forbid"
