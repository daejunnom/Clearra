# Cargo is an ordinary developer tool.  Its default output still belongs to the
# repository's declared build root, so direct cargo commands cannot create a
# repository-local `target/` directory.  Callers may override CARGO_TARGET_DIR
# with another declared Clearra build/cache root when isolation is useful.
[build]
target-dir = "build/cargo/default"
incremental = true
