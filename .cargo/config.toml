# The managed owner supplies CARGO_TARGET_DIR, the platform guard and transaction.
# Unmanaged Cargo fails before compilation instead of creating another target.
# This prevents accidental bypass; it is not an operating-system sandbox.
[build]
rustc-wrapper = "clearra-build-root-required"
incremental = false
