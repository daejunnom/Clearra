# Clearra v0.8.1 Legal-board·BuildUp 가속 및 v0.9.0·v0.9.x PC4 Tablebase 통합 구현·배포 계획

## 1. 문서 권위, 기준선, 상태 모델

이 문서는 v0.8.1 이후의 현재 상위 구현·자격·배포 권위다. 이전 문서
[`v0.8.1-v0.9.x-implementation-release-plan.md`](v0.8.1-v0.9.x-implementation-release-plan.md)는
역사와 증거를 보존하며, 그 문서의 좁은 완료 표시를 새 요구의 완료로 확대하지
않는다. `docs/legal-board/`의 설계, 문제 대장, A/B 계획, Wirelyre 차이 분석은
이 문서의 세부 부록이다.

- 계획 기준선: 원격 `codex/converge-v081-v090-performance-20260921`
  `d3f0586e9adba0e2cfb875160dc1e08f5ebc8f11`
- 연구 기록, 과거 CI 및 benchmark receipt는 출처일 뿐 현재 릴리스 권위가 아니다.
- synthetic fixture나 한 번 성공한 실행은 실제 profile/generation/domain 자격을
  대신하지 않는다.

모든 대장은 다음 상태를 구분한다.

| 상태 | 뜻 |
| --- | --- |
| `Implemented` | 코드와 책임 경계가 존재한다. |
| `Locally validated` | 집중 또는 비게시 로컬 검증이 통과했다. |
| `Qualified` | 실제 profile/generation/domain의 완전성과 정확성이 증명됐다. |
| `Released` | exact source/artifact의 배포와 대표 surface readback이 완료됐다. |
| `Open` | 위 권위 중 필요한 단계가 남았다. |

문서 복원 시점에 새 범위는 모두 `Open`이다. 기존 `[x]`는 원문이 말한 좁은
구현·검증만 보존한다.

### 1.1 현재 구현 스냅샷

아래 표는 계획 전체의 완료표가 아니라 현재 소스에서 확인한 좁은 경계다. `Locally
validated`는 구조·집중 테스트만 뜻하며 실제 자산의 exhaustive qualification이나
릴리스를 뜻하지 않는다.

| 경계 | Implemented | Locally validated | Qualified | Released |
| --- | --- | --- | --- | --- |
| `OriginalRowFrame` 단일 codec과 replay 원행 대응(LB-001) | 예 | 삭제 행 16개 조합 및 bundle round-trip | 아니요 | 아니요 |
| provider 상태·verified absence 분리(LB-002) | 예 | sparse miss, out-of-scope, corruption 대조 | 아니요 | 아니요 |
| PC completion capability와 목표 독립 relation 분리(LB-003) | 예 | scope/binding fail-open 집중 검증 | 아니요 | 아니요 |
| profile·shape·spawn·boundary·lock·clear·좌표 fingerprint(LB-004) | 예 | profile별 identity 및 의미 변이 집중 검증 | 아니요 | 아니요 |
| exact `F∩R` 생성기와 압축 bundle parser | 예 | seed/backtrace/subset/partition 구조 검증 | 아니요 | 아니요 |
| sparse conditioned-reachability 생성기와 parser | 예; 생성 중 모든 선택 레코드에 독립 primitive 대조 연결 | canonical query 및 exact-record/miss 구조 검증, 다섯 profile의 선택 레코드 대조 테스트 실행 | 아니요 | 아니요 |
| 원시 shape·ordered kick 기반 독립 spawn-to-lock 기준 탐색 | bounded differential 테스트와 local-only 생성기 전수 레코드 대조 소스 추가 | 작은 보드 0~2장애물 및 제품 폭 1~6높이의 다섯 profile 차분 테스트 실행 | 아니요 | 아니요 |
| 서명 catalog·immutable native store·CLI lifecycle | 예 | 서명 없는 슬롯 거절, five-slot 및 무-network check, CLI accelerator 집중 테스트 6개 | 아니요 | 아니요 |
| catalog resident-size 증명과 native 동일-generation 재사용 | 예 | payload/상주 크기 불일치 거절, process 내 immutable owner fast path, 두 제품의 합산 한도 mutation 직렬화 | 아니요 | 아니요 |
| 요청별 독립 on/off와 session generation snapshot | 예 | Native CLI·공유 command·Core 집중 테스트 | 아니요 | 아니요 |
| `build.*`/`setup.score` exact accelerator 정책 전달 | BuildV2의 실제 Build query 정책을 native activation에 노출하고 Setup-score coverage·continuation의 두 스위치를 일치시킴. 공통 CLI/Web 파서도 두 스위치를 각각 수용하고 상충 지정은 거부. Build 도움말과 Setup-score 전용 도움말에 옵션 계약 반영 | 두 제품의 4가지 스위치 조합을 분리된 App ingress 통합 테스트로 실행·통과; 공통 명령 파서 집중 테스트 2개 통과. 도움말 테스트는 Windows 정책의 build-script 실행 차단으로 미실행; continuation 내부 unit-test는 타입 검사만 완료(단일 App unit-test 바이너리 LLVM OOM) | 아니요 | 아니요 |
| PC4 CLI 명시 offline fallback | 예 | `--tablebase --offline-fallback` 동일 요청 재구성, 취소 미재시작 | 아니요 | 아니요 |
| v0.8.1 canonical release fail-closed gate | 예; 기존 sparse spawn-to-lock 자격을 국소 entry/exit 자격으로 오인하는 경로도 거절 | 현 시점 Rust 재실행 미완; 다섯 `not_qualified`와 의미 계약 불일치를 소스에서 확인 | 해당 없음 | 아니요 |
| Web PC·Setup·구축 확률 GUI의 profile별 명시 다운로드·취소·삭제와 독립 OPFS namespace | 예 | Web 계약 타입 검사 및 Svelte 구문 검사 | 아니요 | 아니요 |
| Web OPFS 손상·누락 자산 복구 | 예 | 모의 OPFS에서 digest 불일치, 누락 payload, 손상 pointer의 재다운로드·삭제 경계와 저장 도중 취소 시 기존 pointer 보존·새 staging 정리 | 아니요 | 아니요 |
| Desktop 명시 자산 관리와 native 탐색 전 설치 profile 활성화 | Tauri 작업 ID·취소·진행률, CLI 공통 저장소·서명 검증 호출, GUI request의 선택 profile activation 연결 | CLI/GUI host 및 Tauri Rust 타입 검사, Tauri 다운로드 슬롯 집중 테스트 2개, Desktop Svelte compile, managed Desktop 실험용 전체 Vite build; qualified 자산 smoke 미완 | 아니요 | 아니요 |
| WASM의 서명 catalog 조회·payload 재자격·root owner 설치 | 예 | Web 계약 타입 검사만; Windows UMCI로 Rust 실행 검증 미완 | 아니요 | 아니요 |
| 분산 verifier의 단일 공유 자산 owner와 모든 worker의 효과 | 아니요 | 아니요 | 아니요 | 아니요 |
| Discord immutable compute data layer 연결 | 아니요 | 아니요 | 아니요 | 아니요 |
| 국소 entry/exit 후보 binary와 독립 collision-closure 감사 | 구형 sparse pack과 다른 `CLLR0002` parser, 다섯 profile 및 삭제 원행 frame 구분·물리행↔원행 매핑, 원시 shape·ordered kick 기준의 레코드별 관계/의존 마스크 감사 소스 | `conditioned_local` 11개와 독립 reachability reference 10개 Rust 집중 테스트 통과; 완전 domain 및 서명 자산 없음 | 아니요 | 아니요 |
| 국소 entry/first-exit 후보 생성기 | 독립된 질의 schema·profile/원행 frame binding, `CLLR0002` 직렬화와 전 레코드 primitive 감사, `candidate_unqualified` catalog, 별도 bounded WSL 진입점 | SHA `13f02eb7`의 비게시 CI에서 workspace Rust 타입 검사와 집중 후보 테스트 통과; 실생성·완전성 미검증 | 아니요 | 아니요 |
| 국소 관계의 전역 출구 합성 | 국소 lock과 모든 first exit에서 이어지는 완전 보드 탐색을 결합하는 정확 reference 함수; 창 밖 우회·재진입을 배제하지 않음 | 다섯 profile·1~6L 차분을 포함한 Core 집중 11개가 SHA `c292fcc8`의 비게시 CI에서 통과 | 아니요 | 아니요 |
| profile별 실제 legal-board/relation pack | 다섯 legal-board 후보 bundle과 국소 relation 후보 생성기 존재 | 동일 후보 실행 파일의 다섯 profile 전체 P7P4 identity·coverage 차분 통과; 모든 relation 제품 자격 미완 | 다섯 모두 미자격 | 아니요 |
| 성능·메모리 ABBA 및 세 surface parity | 아니요 | 아니요 | 아니요 | 아니요 |

현재 checked-in catalog의 SRS, SRS+, SRS-X, Jstris 180, no-kick은 두 제품 모두
`not_qualified`다. 따라서 이 스냅샷은 v0.8.1 **No-Go**이며, release acceptance는
두 catalog의 모든 슬롯, profile별 합산 128 MiB, 제품별 총량을 통과하기 전
fail-closed한다. 개발·비게시 테스트는 후보 생성과 구조 검증을 계속할 수 있다.
현재 `BoardConditionedReachability` 제품 catalog/parser의 범위는
`spawn-to-lock` sparse record이며, 계획의 실제 entry→first-exit 국소
관계가 아니다. 기존 게이트는 이 catalog 다섯 슬롯이 qualified가 되면
잘못 통과할 수 있었으므로 의미 계약 검사를 추가했다. 현재 이 검사는
`conditioned_local_entry_exit_product_not_implemented`로 의도적으로
No-Go를 유지한다. 현 시점의 다섯 `not_qualified` 오류는 먼저 보고하며,
모두 qualified가 되더라도 이 의미 검사를 통과해야 한다. 나중에 새 국소
관계 제품을 구현할 때에는 실제 parser,
전역 composer, signed catalog 및 독립 자격까지 연결한 뒤 이 검사를
대체해야 한다. 문자열 변경만으로 완료 처리해서는 안 된다.

legal-board 후보 생성용 WSL guest는 관리 manifest에 `legal-board-generate`로 등록해
전용 runtime, 7,200초 상한 및 검증된 `--layers` 출력 경계에 묶었다. 조건부
도달성 pack에는 별도의 `conditioned-reachability-generate` guest/manifest 경계를
추가해 입력 query 파일과 pack/catalog 출력 위치를 각각 검증한다. 셸/manifest
계약 테스트 16개가 통과했다. 이는 **실제 5-profile 생성이나 자격 완료의
증거가 아니다.** 현재 C:에서는 실행 중인 Cargo 소유자가 없고 경로가 작업트리
내 일반 디렉터리임을 확인한 뒤 재생성 가능한 dev 빌드 캐시 45.1 GiB만
`cargo clean --profile dev`로 제거했다. 제품·실험 보존 슬롯과 자산 데이터는
삭제하지 않았다. 2026-09-23 추가 재고에서 별도 과거 작업트리의
`AppData/Local/Clearra/worktrees/v080-options-parallelism-hotfix-20260920/target`
40,564개 파일·약 52.65GiB가 남아 있었고 활성 Cargo/rustc 빌드는 관측되지 않았다.
2026-09-23 사용자 요청에 따라 해당 정확한 경로의 소유·reparse·활성 프로세스를
재확인한 뒤 Cargo의 정리 명령으로 이 재생성 가능 산출물만 제거했다.
C: 여유 공간은 약 97.7 GiB로 늘었다. 다른 작업트리와 제품 자산은 삭제하지 않았다.
WSL의 기본 소스는 계속 Git 추적 파일로 한정한다. 세 local-only 생성
진입점의 미추적 소스만 manifest의 정확한 working-copy allowlist에 넣고,
정책 로드에서 그 목록 외 추가를 거절한다. 특히 새 entry/first-exit 생성기와
기존 sparse spawn-to-lock 생성기는 별도 실행·catalog schema를 가지며 자격을
서로 상속하지 않는다. 저장소 밖·symlink·비밀 경로는 읽기 전에 거절하고,
staging 실패 시 임시 archive를 정리한다. 셸/manifest 계약 테스트 17개와
SHA `13f02eb7`의 비게시 CI에서 Rust manager 빌드·workspace 타입 검사·
국소 관계 집중 테스트가 통과했다. 같은 SHA의 CI 관리기 실행 파일은 로컬에서
storage audit까지 통과했지만, 등록된 WSL 확인은 `Clearra-Build` 배포판 부재로
실패했다. 실제 WSL 생성·archive 전달은 아직 검증되지 않았다.
이 source 경계와 선택 질의 후보는 release provenance나 완전 domain 자격이
아니다. 실제 생성 전에는 archive 입력과 정확한 생성 SHA를 다시 확인한다.
이 작업트리의 `AGENTS.md`는 Rust `tools/clearra-manage`를 활성 관리기로
정하므로, 존재하지 않는 역사적 Python 관리기를 대체 실행하거나 raw WSL로
관리기를 우회하지 않는다.

`F_k` 생성기의 층 파일 읽기는 원본 파일 전체 바이트와 파싱된 `u64` 상태 벡터를
동시에 보유하던 경계에서 순차 해시·검증·파싱으로 변경했다. 이 변경은 해당
중복 버퍼를 없앤다. 또한 전방 생성의 worker별 전체 `BTreeSet`을 1,024개
source 상태 단위로 제한하고, input·filter digest에 결박된 임시 run을 재개·병합해
최종 집합을 정확히 중복 제거하도록 변경했다. 다만 큰 `F_k` 자체와 중간
disk peak, 32-run 병합 시 한 그룹의 일시적 벡터 문제를 해결했다는 뜻은
아니다. Wirelyre의 정방향 그래프는
isolated-cell 및 split 조건을 사용하지만, Clearra는 중간 행 삭제가 가능한
물리 보드·원행 codec을 사용한다. 동일 prune의 안전성이 증명되기 전에는
특히 음성 판정에 이를 이식하지 않는다. 전방 spill의 기능 검증과 실제 다섯
profile 생성·공간 계측은 계속 `Open`이다.

전방·후방 pair run을 최대 32개씩 줄이는 병합 단계가 결과 전체를 `Vec`에
올린 뒤 파일로 다시 쓰던 경계는 입력 reader와 1 MiB writer만 유지하는
스트리밍 병합으로 바꿨다. 출력 count·digest는 파일 헤더에 뒤늦게 기록하고,
완전 기록·동기화 뒤 기존 파일을 덮어쓰지 않는 동일 디렉터리 게시를 한다.
중복 pair, 정렬 및 digest round-trip 테스트를 소스에 추가했다. 이어서 최종
전방 병합도 정렬된 필드를 domain 파일에 바로 쓰도록 변경하고, count를 확정한
후 헤더와 전체 파일 digest를 완성하는 경계를 추가했다. 기존 직접 작성 파일과
바이트 단위 동등성, 재개, 잘못 정렬된 결과의 게시 거부 테스트를 소스에
추가했다. 제품 생성에서 쓰는 필터 없는 전방 `F_k → F_(k+1)` 단계는 입력
파일을 먼저 전수 검증해 digest를 고정하고, 두 번째 순차 읽기에서 bounded
source chunk를 생성한 뒤 digest를 다시 대조한다. 따라서 이 단계는 입력과
출력 `F_k`의 전체 상태 벡터를 동시에 또는 각각 적재하지 않는다. 후방 `R_k`
생성과 `L_k` 역추적의 최종 검증 결과도 bounded checkpoint에서 domain
writer로 바로 전달해 전체 출력 벡터를 제거했다. 직접 작성 파일과의 바이트
동등성 테스트를 소스에 추가했으나 실행하지 못했다. 역사적 필터 적용 전방
단계와 `L_k` 역추적의 목표 층은 아직 전체 입력 벡터를 적재한다. `L_k`의
`F_k` membership은 검증된 정렬 파일의 순차 커서로 바꾸고, 전역 pair-run
병합 후 한 번의 exact 교집합에서 판정한다. 입력 전체의 재검증 digest가
일치해야만 결과를 게시한다. 이 초기 버전은 `F_k`에서 탈락할 원시
predecessor도 pair run에 먼저 기록해 중간 디스크 피크를 키울 수 있었다.
후술한 Bloom 후보는 이 지점만 줄이며 exact 커서 검증을 제거하지 않는다.
생성기 전체의 메모리·공간 한도 해결로 판정하지 않는다.
후보 bundle 게시 때에도 각 `L_k`의 complete `F_k`/`R_k` 부분집합 검사는
두 파일의 정렬 커서로 수행한다. 인코더는 같은 불변 포맷을 유지하면서
`L_0…L_10`을 층별 검증 방문자로 받아 전체 층 벡터의 동시 보유를 제거했다.
기존 인코더와의 바이트 단위 동등성 테스트는 소스에 추가했지만 실행 전이다.
압축 결과 자체는 최대 64 MiB 메모리에 보유하며, `Arc` 변환·parser index까지
포함한 생성 시 순간 peak와 active-session 공유 128 MiB는 실측 전이다.
Rust 실행·실제 프로필 검증 전이므로 자격 상태는
계속 `Open`이다. 집중 테스트의 Windows 최초 실행과 한 번의 수동 재시도는
모두 `generic-array` build script가 애플리케이션 제어 정책 `os error 4551`로
차단돼 테스트 본문까지 도달하지 못했다. 자동 반복이나 WSL 우회는 하지 않았다.

후보 생성은 양방향 정확 교집합으로 변경했다. 초기 후보는 6피스 층에서
만나도록 했으나, 실제 SRS+의 `F_4`가 13,299,035개임을 확인한 뒤
무제한 정방향 frontier를 한 층 줄이는 **5피스 meet 후보**로 바꿨다.
즉 완전한 `F_0…F_5`, 완전한 `R_10…R_6`을 독립 생성하고,
`L_5 = F_5 ∩ Pre(R_6)`을 exact ILC로 계산한다. 아래층은
`L_k = F_k ∩ Pre(L_{k+1})`, 위층은 `L_{k+1} = Post(L_k) ∩ R_{k+1}`로
만든다. 후자는 `F_{k+1}` 전체 생성이 아니다. 두 등식은 `F`·`R`의
전이 정의에서 귀납적으로 따르며, 행 삭제를 포함한 같은 exact edge 관계를
양방향에서 사용해야만 성립한다. 후보 bundle 게시 직전에는 각 `L_k`의
source·filter 파일 digest가 이 전방·후방 점화식의 이웃 층과 정확히 이어지는지
검사한다. 기존 `forward-reachable-layer-07…10`은
역사적 중간물로 남겨도 새 bundle 체인에는 쓰지 않는다. 초기 6피스 meet
후보의 소스와 국소 관계 차분은 SHA `c292fcc8`의 비게시 CI에서 컴파일·집중
테스트를 통과했고, 그 바이너리로 SRS+ `F_0…F_5`까지 생성했다. 이후
5피스 meet 소스로 `R_6`와 `L_5`까지 진행했으나 최종 bundle과 다섯
프로필 자격은 아직 검증 전이다. 어느 쪽도 `Qualified`가 아니다.
후속 SHA `df42e7a1`의 비게시 Windows·Linux 관리 게이트는 통과했고,
그 정확한 Windows 후보 생성기를 로컬에 확보했다. SRS+ 단일 프로필의
중간 층은 `F_5` 151,891,476개(층 파일 약 1.13 GiB), `R_7`
2,022,317개, `R_6` 52,918,372개(약 0.39 GiB)까지 생성·재읽기
검증했다. `R_6` 생성은 약 1억 5,429만 서로 다른 기하 후보를 exact ILC로
확인해야 했고, 중간 작업 공간은 약 9 GiB까지 증가했다. 이는 생성기 진행
증거일 뿐 다섯 프로필 또는 크기·성능 자격의 완료가 아니다.
다음 `F_5 ∩ Pre(R_6)`은 원래 모든 원시 predecessor를 pair run에 먼저
기록했다. 이를 완전 검증한 `F_5` 파일에서 구성하는 최대 128 MiB의
no-false-negative Bloom semi-join 후보로 줄이고, 최종 승인 여부는 여전히
정렬된 `F_5` 커서와 exact ILC에 맡기도록 소스를 변경했다. 이전 spill과
섞이지 않도록 체크포인트 identity도 바꿨다.
Bloom이 거르는 `L_5` 전용 pair-run 청크는 512개에서 4,096개 target으로
늘렸다. 이 변경은 일반 역방향 층의 청크 크기를 바꾸지 않고 체크포인트
파일 수를 줄인다. SHA `077560db`의 비게시 Windows·Linux 관리 게이트와
Bloom no-false-negative·청크 경계 exact 차분 테스트가 통과했다. 그 exact
Windows 생성기로 SRS+ `L_5` 6,830,860개를 생성하고 재읽기 검증했다.
이 층에서 34,558,011개의 `F_5` 생존 후보 쌍을 exact ILC로 확인했고,
관리 영수증은 약 362초, peak 약 551 MiB, 메모리 압박 이벤트 0회였다.
이어 `L_4` 2,557,506개, `L_3` 273,489개, `L_2` 10,191개,
`L_1` 162개, `L_0` 빈 필드 1개를 같은 체인에서 생성했다. 이어
`L_6` 4,796,285개, `L_7` 754,994개, `L_8` 19,412개,
`L_9` 100개, `L_10` 완성 필드 1개를 정방향 필터로 생성했다.
전체 proof-chain의 층별 source/filter digest와 부분집합 검사를 통과한
SRS+ 단일 프로필 후보 bundle은 25,367,054바이트(약 24.2 MiB)로
프로필당 64 MiB 상한 안에 들었다. catalog는 여전히 명시적으로
`candidate_unqualified`이며, 독립적인 실제 profile KAT, 나머지 네
프로필의 생성·크기, 서명 production generation, 공유 peak 및 제품 성능
자격은 `Open`이다.
같은 SHA `077560db` 생성기를 Jstris 180에 독립 적용해 `F_5`
151,891,476개, `R_6` 52,237,651개와 `L_0…L_10`을 완주했다.
교집합층 `L_5`는 6,805,146개이며 나머지 층의 개수는 순서대로
1, 162, 10,191, 273,459, 2,554,536, 4,769,952, 752,753,
19,405, 100, 1개다. 최종 후보 bundle은 25,276,473바이트이고
SHA-256 `e98afb1fdf5cf2f058b4303192b382f4b39295ff917919428ca5721077454739`가
catalog 선언과 일치한다. 두 번째 profile의 생성·재읽기·크기 증거일 뿐,
catalog는 `candidate_unqualified`이고 독립적인 전체 해법 KAT·제품
서명·성능 채택은 여전히 `Open`이다. 전체 해법 차분용 ignored local-only
KAT의 **컴파일**은 SHA `8a5a0bbc`의 Windows·Linux 관리 CI에서 통과했다.
키 방향 수정 후 Jstris 180도 기존 `F_0…F_5`, `R_6…R_10`,
`L_0…L_10`의 source/filter digest와 부분집합 연결을 현재 생성기로
다시 검증해 별도 v2 후보 bundle을 만들었다. 새 파일은
`legal-board-jstris-180-v2.cllb`, 25,276,473바이트, SHA-256
`7d20636d3f6e26415dcac7afda161f34916848e1cbc165ca030030eda087d30a`다.
새 층은 생성하지 않았고 기존 v1 파일은 보존했다. catalog는 계속
`candidate_unqualified`이며 전체 456,459개 identity 차분, signed asset,
제품 설치 및 성능 자격은 이 단계에서 실행하거나 부여하지 않았다.
CLI의 명시 `legal-board generate`도 이제 v2 후보 이름으로 출력해 같은
프로필 디렉터리에 남은 v1 후보를 덮어쓰거나 충돌하지 않는다. 명시
`remove`의 소유 파일 목록에는 v2 후보와 `reverse-filter-layer-*`를
포함했다. 실제 사용자 자산 삭제 명령은 실행하지 않았으며 v2 경로
집중 테스트만 통과했다.
생성기 자체도 `--bundle`·`--catalog`가 검증된 `--layers`와 같은 실제
디렉터리에 있을 때만 출력하도록 제한했다. 다른 디렉터리로 빠지는
후보를 거절하는 집중 테스트를 통과했다. 이는 저장소 관리기의 허용
루트 검사를 대체하지 않는다.
역방향 source 검증은 종전의 전체 목표 열거·정렬·벡터 변환 대신
충돌 없는 기하학적 배치에서 입력 층으로 이어질 수 있는 잠금 위치를 먼저
모은다. 이 집합이 비면 BFS를 생략하고, 비어 있지 않으면 같은 exact
first-success ILC BFS에서 첫 도달 잠금만 검증한다. profile·미노·물리 높이의
규칙 템플릿과 BFS scratch는 검증 워커 내부에서만 재사용한다. 다섯
profile의 Board64 목표와 Hydra hash 포함 판정이 종전 전체 열거와
같은지, 기하학적으로만 가능한 좁은 지붕 아래의 잠금이 음성으로 남는지,
기존 reachability 경계가 보존되는지 집중 테스트를 통과했다. 다만
기하학적 목표 전부에 대한 추가 차분 테스트의 로컬 실행은 Windows
애플리케이션 제어가 새 테스트 실행 파일을 두 번 차단했다. 대신 exact SHA
`d064841a2e7ac24cf2106b667f23809e313b5ba6`의 브랜치 전용 비게시 CI
run `35837283321`에서 해당 차분, 막힌 지붕의 음성 판정, Hydra hash
교차 검사와 기존 reachability 테스트가 모두 통과했다. 이 CI는 source
contract 증거이지 실제 전체 자산 자격이나 release acceptance가 아니다. 당시
SRS 생성 프로세스는 변경 전 실행 파일로 진행했으므로 이 수정은
완료한 층을 덮어쓰지 않고 다음 재개·프로필 생성에만 적용한다. 자산
완전성이나 성능 자격을 이 소스 테스트만으로 부여하지 않는다.
입력 역방향 목표 층에 대한 소형 Bloom admission을 검증 배치 전체에서
재사용하도록 소스를 추가했다. 필터가 음성인 기하학적 목표는 sorted-domain
이진 검색을 건너뛰되, 양성은 반드시 원래의 exact 이진 검색으로 확인한다.
허위 음성은 허용하지 않으며 속도 이득은 후속 동일 바이너리 A/B 전까지
주장하지 않는다. 이전 SRS 실행 파일에는 이 수정이 적용되지 않는다.
source SHA `c1c57cc676cf9930b38154ff032278855c9c4d3f`의 비게시
accelerator CI run `35839641030`과 management CI run `35839641002`는
모두 성공했다. 이 성공은 target admission과 국소 relation의 source
contract 검사이며 후보 자산 전체 자격이나 성능 채택 증거가 아니다.
일반 SRS의 사용자 제공 438,791개 oracle은 exact SHA
`c1c57cc676cf9930b38154ff032278855c9c4d3f`에서 만든 Windows
최적화 KAT의 관리 실행(`profile=srs`, 4 worker, baseline-only)에서
실제 438,791개와 `cts1:e7de12adf5a07b7e`로 일치했다. 후보
legal-board를 설치하지 않은 기준 solver 증거이므로, 완성 후보와의
전체 identity 차분이나 자산 자격을 대신하지 않는다. 먼저 실행한
debug SRS 생성은 역방향 6단계에서 관리 시간 한도로 종료됐고,
완료된 checkpoint를 같은 exact SHA의 최적화 생성기가 재검증·재사용하며
이어받았다. 재개 시점만으로는 bundle 완성을 주장하지 않았다.
이어받은 생성은 SRS의 `L_0…L_10` 체인과 v2 후보 bundle 게시까지
완료했다. bundle은 23,749,029바이트, SHA-256은
`0dee2f88b01c8f44df8c2e5f3d5b7f6b76feb772b4e2fb7b7daab4165b155160`이며
재읽기 공유 인덱스는 25,418,640바이트다. 같은 exact SHA의 독립
4-worker 전체 P7P4 차분에서 baseline과 후보 필터가 모두 **438,791**개,
`cts1:e7de12adf5a07b7e`를 반환했고 normalized identity 순서 및
coverage도 완전히 일치했다. 이는 SRS 한 프로필의 실제 전체 집합
`Locally validated` 증거다. 직렬 두 실행의 시간은 성능 채택 ABBA가 아니며,
서명·배포·다섯 profile 자격까지 이 결과만으로 닫지 않는다.
같은 `c1c57cc6` 최적화 생성기로 SRS-X의 11개 `L_k` 층과 v2 후보
bundle도 완성했다. 크기는 33,428,297바이트, SHA-256은
`d6ed2ef22dfe60cf98708360e98743c003aaff53f4c559b8bbfbb44ff0b371e6`이며,
재읽기 인덱스는 27,646,992바이트다. 같은 source의 4-worker 전체
P7P4 차분에서 기준 및 후보 적용 경로가 모두 **529,730**개와
`cts1:3e20f1fe11051463`을 반환했다. 테스트는 normalized identity
배열과 coverage 배열까지 일치시켰다. 이것은 SRS-X의 해당 전체 집합
`Locally validated` 증거일 뿐, 모든 입력의 exhaustive 자격·서명·성능
채택을 닫지 않는다. 각 경로의 직렬 실행 시간도 사용자 요청에 따라
후속 ABBA 전에는 성능 결론으로 사용하지 않는다.
동일 `c1c57cc6` 후보 실행 파일의 나머지 세 profile 차분도 완료했다.
Jstris 180은 25,276,473바이트 bundle
(`sha256:7d20636d3f6e26415dcac7afda161f34916848e1cbc165ca030030eda087d30a`,
인덱스 25,418,640바이트)에서 기준·후보 모두 사용자 제공 oracle과 같은
**456,459**개 및 `cts1:2fcbc950acdffa31`이었다. no-kick은
12,669,421바이트 bundle
(`sha256:956b55b8797b37fe3470ee17a497e664b7151f559c8db280ed510e179b85b669`,
인덱스 10,359,696바이트)에서 둘 다 **238,368**개 및
`cts1:da73cb0ed368c7b7`이었다. SRS+도 같은 실행 파일로 재검증해
기준·후보 모두 **456,923**개 및 `cts1:98ebe8726537b29f`였다.
각 실행은 normalized identity 전체 순서와 coverage 배열까지 같았다.
다섯 후보의 압축 bundle 총합은 120,490,274바이트로 320MiB 합계
상한 이내이고 최대 단일 후보인 SRS-X의 번들+인덱스도 128MiB 이하이다.
하지만 이는 한 P7P4 family의 `Locally validated` 집합 차분이며,
전체 profile domain의 exhaustive 자격, 조건부 도달성 제품 pack,
서명 generation, 세 surface, 후속 성능 ABBA를 완료했다는 뜻이 아니다.
서명 키 정책은 사용자가 Oracle SSH 접속 키와 별개의 신규 Ed25519 전용
키 생성 및 GitHub Secret 등록을 승인했다. 실제 다섯 자산의 자격 검증
전에는 개인키를 생성·유통하거나 candidate catalog에 제품 서명을 붙이지
않는다. 공개키·key ID만 소스에 두고 개인키는 채팅·저장소·로그에 노출하지 않는다.
전체 해법 차분 KAT를 SHA `06c77f86`에서 CI가 만든 최적화 Windows 실행 파일로
SRS+ 후보에 실제 실행하자 관측 개수 456,922가 기준 456,923과 1개
달라 **실패**했다. 첫 실행은 단계별 개수를 기록하지 않아 기준 solver와
legal-board 필터 중 어느 쪽에서 차이가 났는지 판정하지 못했다. 이어
SHA `5bd3016f`의 CI 최적화 실행 파일로 4-worker, 두 전체 탐색을 직렬
재현하자 기준 solver는 **456,923**개와 기준 해시
`cts1:98ebe8726537b29f`, 후보 필터는 **456,922**개와 다른 해시를
반환했다. 차집합은 누락 identity 정확히 하나이고 추가 identity는 없다.
두 탐색 합계 460.81초는 단일 P7P4 탐색 시간으로 인용하지 않는다.
관리 영수증의 peak는 약 3.15 GB이며 memory-pressure event는 0회였다.
해당 누락 identity의 4×10 그림은 Git에 넣지 않은
`_local/artifacts/v081-legal-board/srs-plus/missing-solution-456923.png`에
보존했다. 이 후보의 음성 판정은 실제 false negative이므로 **No-Go**다.
누락 해법의 원래 행/삭제 행 투영, graph transition, 층별 membership을
분리하는 작은 local-only 진단을 먼저 수행하고 수정 후 전체 집합 차분을
다시 실행한다. 실패한 bundle을 자격 자산으로 설치하지 않는다.

후속 원인 분리에서 누락 identity의 6번째 상태 `f8303d1bff`는 생성된
SRS+ `reverse-filter-layer-06.bin`과 `legal-layer-06.bin` 모두에 있었다.
그러나 같은 상태를 요청한 `ExactLegalBoard::decide`는 `VerifiedAbsent`를
반환했다. 원인은 최종 layer/bundle의 각 10비트 행이 우→좌 storage key
(`07f0362fff`)로 정렬·인코딩돼 있는데 조회는 좌→우 Clearra Board64
(`f8303d1bff`)를 그대로 사용한 I/O 경계 불일치다. 보드 집합이 대체로
거울 대칭이어도 SRS+의 순서 있는 180 킥은 거울 대칭을 보장하지 않으므로
이 오류는 실제 한 해법의 음성 오판으로 나타났다. `tetrio.js`의 SRS+
JLSTZ 180 오프셋은 J/L이 같은 표를 쓰고 0→2의 오른쪽 후보가 왼쪽
후보보다 앞선다. Clearra의 표도 이 순서와 y축 반전을 일치시켰으므로,
증거가 없는 킥 수치 변경은 하지 않는다.
대조한 로컬 원본 `tetrio.js`의 SHA-256은
`828297b28b02a0fd4b872fde40497ea8a079e20e1e768457a5f67e17585a830`이다.
`GetPieceKickset`상 표준 J/L에는 별도 특수 kickset이 없고, `ComputeKick`은
무이동 회전을 먼저 시험한 뒤 표의 후보를 순서대로 검사해 첫 성공에서
종료한다. 따라서 J/L의 기하학적 거울 변환만으로 legal 상태 집합의 거울
대칭을 가정할 수 없다. 이 사례에서 비대칭의 유일한 원인이 J/L 180인지는
별도로 증명되지 않았으며, 확정된 결함은 저장 키와 조회 키의 불일치다.
실제 누락 identity의 보고된 전반부 S→J1→Z1→O→T2→I는
모두 정확한 graph edge이며, 그 직후 J2는 `rotation=Two, x=5, y=0`의
concrete placement로 materialize된다. 따라서 사용자가 지적한 180
회전 위치를 재현했다. 보고된 후반부의 특정 L 배치 순서는 독립 graph
검사에서 edge가 아니었으므로 완성 replay 순서로 단정하지 않는다.
대신 같은 placement family가 다른 정확한 graph 경로로 완성 필드까지
연결됨을 확인했다. 어떤 경로에서도 위 6번째 상태의 lookup은 동일하게
잘못된 음성 판정을 내렸다.

수정 후보는 legal-board lookup에서만 행별 bit order를 변환하고,
bundle magic/version과 generation identity를 v2로 올려 이전 후보가
암묵적으로 재사용되지 않게 한다. `OriginalRowFrame`은 원래 행 대응을
유지한다. 비대칭 6번째 상태의 focused 회귀 테스트와 관련 parser/
scope 테스트는 로컬 통과했으나, v2 bundle을 생성해 **전체 456,923개
identity**와 비교하기 전에는 false-negative 해결을 `Qualified`로
간주하지 않는다. 당시에는 전체 집합 차분 전이므로 기존 v1 bundle과
layer 중간 파일을 임시로 남겼다. 현재 다섯 profile의 P7P4 차분 이후에는
최종 v2 bundle·후보 catalog와 검증 기록만 유지하고, 재생성 가능한
층별 중간 파일·구형 v1 후보는 정리 대상이다. 중간 파일의 영구 보존을
사용자 요구로 해석하지 않는다. 현 로컬 산출물 115개(약 9.01GiB)의
정확한 삭제 대상은 확인했지만 실행 정책이 삭제 명령을 거절했으므로,
이 기록 시점에는 실제 파일이 남아 있으며 다른 수단으로 우회하지 않았다.
SHA `90c62414`의 비게시 Windows CI에서 생성기와 최적화 KAT 실행 파일을
다시 만들었고 관리 CI도 성공했다. Windows 앱 제어가 CI 생성 실행 파일을
두 번 차단해, 같은 소스의 로컬 debug 생성기로 **기존 층을 재탐색하지
않고** 전체 digest/subset을 재검증해 v2 bundle을 별도 파일로 게시했다.
크기는 이전과 같은 25,367,054바이트이고 SHA-256은
`e830202b61034a3dcdf7c0e03cb3b94a5bbd06a6e0b651776a5a14d6b6a6e7d2`다.
새 bundle에서 누락 identity의 실제 graph 경로 모든 상태가
`CandidateAllowed`로 판정되는 focused 진단은 통과했다. 이는 전체 집합
차분의 대체가 아니므로 asset과 profile은 여전히 `candidate_unqualified`다.
이어 SHA `90c62414`의 동일 최적화 KAT 실행 파일, 4워커, 직렬
baseline→filter로 **전체 집합**을 비교했다. 둘 다 456,923개와
`cts1:98ebe8726537b29f`를 반환해 이 사례의 false negative는
수정됐다. 그러나 baseline은 173.647초, v2 필터는 333.229초였고
BuildOrder 방문은 637,208,005→627,744,804로 약 1.5%만 줄었다.
따라서 이것은 `Locally validated` 정확성 증거이며 성능 `Qualified`
증거가 아니다. 256개 delta를 최대로 선형 디코드하는 sparse checkpoint
조회가 hot BuildUp에서 비용을 키우는지, index stride를 16으로 줄여
동일 bundle에서 집중 A/B를 진행한다. 그 후에도 p95 채택 기준을
통과하지 못하면 구조적 후보/상태 수준의 호출 축소를 검토하고 No-Go를
유지한다.
SHA `c290b224`에서 checkpoint stride를 16으로 바꾼 동일 4-worker
직렬 A/B도 전체 결과 수와 해시가 일치했다. baseline 172.936초,
filter 200.375초로 기존 filter 333.229초보다 크게 개선됐지만 여전히
baseline보다 약 15.9% 느리다. bundle 25,367,054바이트 외의 공유
인덱스는 45,188,096바이트였고 관리 영수증의 메모리 압박은 0회였다.
방문 노드 감소는 여전히 약 1.5%이므로 checkpoint 간격만 더 줄여
20% BuildUp / 10% 전체 이득을 얻는다는 근거가 없다. 정확한 layer를
한 번 스캔해 만든 no-false-negative 음성 필터를 hot path에서 쓰고,
압축 bundle exact lookup은 자격·진단용으로만 남기는 대안을 독립 A/B한다.
음성 필터의 양성은 자격 증명이 아니라 단순 통과이며, false positive는
prune 기회만 놓칠 뿐 결과 집합을 바꾸지 않아야 한다.
그 후보는 완전 검증된 bundle payload를 로드할 때 layer별 64비트
block Bloom을 만들고, BuildUp의 음성 판정에서는 단일 word만 조회한다.
정확한 membership을 요구하는 진단·자격 API는 기존 압축 bundle
lookup을 유지한다. Bloom 거짓 양성을 강제로 만들어도 정확 API가
`VerifiedAbsent`를 반환하는 테스트와 삽입 키 전체의 no-false-negative
bounded 테스트가 통과했다. 이것은 구현·집중 검증 상태이며 실제
P7P4 시간·메모리·동일 identity KAT 전에는 성능 자격을 부여하지 않는다.

별도의 SHA `e216c8eb` 11-worker baseline-only 계측은 약 82초에서
메모리 압박으로 중단됐다. 이는 완료 시간이 아니며 외삽하지 않는다.
종료 시 process-tree peak는 약 6.78 GB, host 물리 여유는 약 500 MB였고
압박 이벤트 1회, GC 확인 0회였다. 이 실행의 큰 worker 수와 다른
호스트 프로세스의 합산 메모리 경합을 분리해야 한다. 과거 8-slot
142.17초 기록은 다른 Chrome/WSL 환경과 과거 SHA라 현재 11-worker
결과로 취급하지 않는다. 선별한 4/7/11-worker 동일 binary 단일 탐색,
실제 CPU 사용량, 단계별 시간과 memory receipt를 기록하되 압박 표본은
censored로 남긴다. 두 최종 후보 bundle은 이미 각각 약 25 MB로 로컬에
저장돼 있어 검색 지연을 곧바로 자료 저장 불능으로 해석하지 않는다.
같은 `e216c8eb` 바이너리의 7-worker baseline-only 실측은 142.708초,
456,923개와 기준 해시 `cts1:98ebe8726537b29f`였고 요청한 7워커가
모두 활성으로 보고됐다. 이는 7-worker의 현 호스트 단일 완료 표본이며
과거 11-worker 약 2분 기록과 동등 조건 비교나 속도 채택 증거가 아니다.
층별 임의 유한 그래프 1,000개에서 양방향 점화식과 전체 `F∩R`의 집합 일치를
확인한 것은 수학적 계약의 보조 검사에 불과하며, 실제 kick/ILC 차분 테스트나
다섯 profile 자격 증거를 대신하지 않는다.

조건부 도달성의 현 pack은 전체 board와 profile sky seed에 대한 sparse Boolean
spawn-to-lock cache이며, 계획의 임의 진입 pose·국소 입출구 macro·witness 관계를
대신하지 않는다. 독립 primitive BFS는 다섯 profile의 ordered kick을 원시 shape
충돌로 해석하여 작은 보드의 장애물 0~2개 조합 및 10폭 1~6높이 표본과 최적화
탐색을 비교하도록 추가했다. local-only 후보 생성기는 입력 query set의 모든
레코드를 이 기준 탐색과 다시 대조하고 불일치 시 게시 전에 실패한다. Windows
수동 재시도에서 두 bounded 차분 테스트와 생성기의 다섯 profile 선택 레코드
대조 테스트가 통과했다. 그러나 query set 밖의 완전성은 증명하지 못했으므로 이 코드나 기존
생성기 결과를 실제 pack의 `Qualified` 증거로 승격하지 않는다.
후보 생성기의 query 파일은 읽기 전에 실제 일반 파일·크기 한도를 확인하고,
기존 출력이 link 또는 다른 내용이면 거절한다. 후보 pack/catalog 게시에는
기존 파일을 덮어쓰지 않는 동일 디렉터리 hard-link 게시를 사용한다. 이는
local-only 파일 수명 보강이지 국소 relation 구현이나 signed release 게시의
증거가 아니다.

국소 relation의 다음 차분 검증을 위해 독립 primitive BFS에 실제 진입 pose
집합을 명시하는 경로를 추가했다. 잘못되거나 충돌하는 진입 pose는 빈 도달
집합으로 조용히 바꾸지 않고 질의 오류로 돌려보낸다. 최적화된 템플릿 BFS에도
동일 시작 pose를 사용하는 테스트 경로를 두고 다섯 profile·일곱 미노를
대조하도록 했다. 이 경로는 전체 물리 보드의 정확 탐색일 뿐 창 밖 우회가
불가능하다는 증명이 아니다. 이후 동일 최적화 탐색을 `derive_exact_conditioned_entry_lock_anchors`라는
공개된 전체 보드 primitive로 승격했다. 빈·유효하지 않은 entry 집합은 완결 음성 판정이
아닌 `None`으로 돌려주고, 다중 entry 집합의 독립 BFS 대조 테스트를 추가했다.
`derive_exact_conditioned_local_relation`에는 전체 물리 보드의 충돌·first-success
결정을 사용하되 탐색을 pose 창으로 제한하고, 최초 창 밖 상태를 모두 출구로 기록하는
entry→exit/lock 원시 관계도 추가했다. 창 밖 우회 후 재진입은 출구를 기존 전체 보드
탐색에 합성해야 보존되므로 출구가 남은 국소 음성 결과만으로 전역 불가능을 선언하지 않는다.
독립 BFS와 국소 lock + 출구 후속 lock의 합이 일치하는 다섯 profile 테스트를 추가했다.
현재 원시 관계에는 창 안의 모든 유효 source pose, translation/grounding target 및
first-success의 **모든 선행 킥 후보**가 읽는 셀을 보수적으로 합친 의존 마스크도
추가했다. 동일 규칙·창·진입 집합에서 마스크상의 점유가 같을 때만 in-process
재사용을 허용하고, 마스크 밖 단일 셀 변이에서 결과가 같은지 대조하는 테스트를
추가했다. 이는 충분조건인 보수적 서명이며 작은 pack이나 높은 적중률의 증거는 아니다.
또한 원시 shape 셀·ordered kick을 직접 해석하는 독립 국소 BFS를 별도 모듈로
구현하여, 단순한 최종 lock 합집합뿐 아니라 창의 **첫 출구 집합 자체**를
다섯 profile·일곱 미노·상단 장애물 표본에서 비교하도록 했다. 생성기에 연결할
qualification oracle의 소스 후보이지, 아직 실행된 차분 증거는 아니다.
별도 `LocalRelationCandidateIndex`는 profile·미노·좌표창·진입 집합을 묶어
in-process 후보 관계를 조회하며, 같은 질의에 겹치는 점유 조건이 서로 다른
lock/exit를 반환하면 설치 전에 거절한다. 이는 충돌 검출용 비권위 후보 인덱스이고
제품 registry 또는 BuildUp hit 경로가 아니다. 별도 `CLLR0002` 후보 바이너리에는
profile별 규칙 fingerprint, payload·generation digest, 정렬된 가변 길이 관계와
16MiB 파일 상한을 묶고, 파서는 길이·중복·충돌을 거절한다. BuildUp의
고정 목표 높이와 삭제된 원래 행 mask를 `LocalRelationRowFrame`으로 분리해
같은 compact physical board라도 다른 원행 대응이면 후보를 재사용하지
않는다. 기존 4L `OriginalRowFrame`의 자격을 1~6L로 암묵 전파하지 않는다. 구형 sparse
spawn-to-lock `CLBR0001`과 parser/namespace를 혼용하지 않는다. 다섯 profile의
라운드트립·손상·세대 불일치 테스트와 `qualification-reference` 전용
레코드 감사 코드를 추가했다. 감사 코드는 raw shape·ordered kick으로 모든
창 내부 source 및 이동/킥 후보의 충돌 의존 셀을 독립 재구성하고, 저장된
source-board lock·첫 exit를 원시 BFS와 대조한다. 이는 저장된 후보 레코드의
오류를 검출하는 단계이며, 모든 가능한 창·진입·보드의 완전성 또는 전역
BuildUp 합성의 정확성을 증명하지 않는다. 현재 소스에서
`conditioned_local` 11개와 독립 `reachability::reference_tests` 10개 Rust
집중 테스트를 실행해 모두 통과했다. 서명된 product authority, 실제 압축
크기·메모리 측정과 제품 설치는 아직 `Open`이다.
국소 관계를 dependency mask가 같은 다른 board에 재사용할 때 출구 후속
탐색을 저장된 원본이 아닌 실제 질의 board에서 수행하도록 수정하고,
독립 primitive BFS와 다시 대조했다. exact SHA `fba6b209`의 비게시
accelerator CI `35847950029`와 management CI `35847950020`이
통과했다. 이는 후보 소스의 재사용 정확성 증거이지 제품 pack 자격이 아니다.
local-only 후보 생성기의 파일 경로도 작은 SRS+ 질의로 실행했다. 독립
레코드 감사, `candidate_unqualified` 표시, 동일 입력 재실행의 byte identity,
다른 내용의 immutable 출력 덮어쓰기 거절을 검증했다. 이 검증은 프로필
전체 query domain의 완전성이나 실제 제품 pack 자격을 대신하지 않는다.
재사용 가능한
결정 DAG, 16MiB 이하의 소형 조건부 relation pack, 임의 entry의 제품
가속 활성화와 witness/스핀 증거는 여전히 `Open`이다.

Web 수명주기는 소스에 내장된 서명 catalog를 WASM에서 검증한 뒤에만 자산 URL과
용량을 제시한다. 사용자가 명시 다운로드를 누르기 전에는 원격 asset을 읽지 않고,
다운로드 후에는 digest와 제품 parser를 통과한 바이트만 별도 OPFS pointer에
게시한다. 검색 root WASM owner는 그 로컬 바이트를 재검증해 설치하며, 다른
profile의 이전 슬롯을 제거한 후 시작한다. 동일 서명 세대가 계속 설치되어 있으면
OPFS pointer와 파일 길이를 재확인하되 큰 payload를 매 검색마다 WASM으로 다시
복사하지 않는다. 명시 삭제·교체·profile 변경 시 이전 WASM slot을 제거한다.
손상된 로컬 pointer나 payload는 서명된 다운로드 계획을 가리지 않고
`InvalidAsset`으로 표시해 재다운로드·삭제를 허용한다. 이 복구는 모의 OPFS 계약
검증 상태이며 실제 브라우저와 qualified Release asset의 smoke를 대신하지 않는다.
현재 분산 verifier는 동일 자산을
복제하지 않는 대신 기존 정확 경로를 계속하므로, 모든 worker가 가속 효과를
얻는 shared-owner 설계와 active-session 실제 peak 검증은 여전히 `Open`이다.
두 제품 registry의 설치 전 128 MiB 검사도 선택 profile만이 아니라 모든
resident profile의 크기를 합산하도록 소스를 보완했다. 다만 Windows 애플리케이션
제어로 이 Rust 경계의 실행 검증은 아직 못 했고, 실제 자산의 decode·install 중
순간 peak 및 worker별 복제량도 별도 실측이 필요하다.
Web의 host snapshot은 기기 메모리가 미확인일 때 WASM 단일 transfer를 32 MiB로,
0.5 GiB 기기에서는 16 MiB로 제한한다. legal-board 자산 상한 64 MiB보다
작으므로 큰 qualified bundle은 설치 상태여도 검색 root에서 활성화되지 않고
정확 fallback으로 돌아갈 수 있다. 다운로드 화면에는 root worker와 같은 host
snapshot의 transfer 한도를 보여 주고, 초과한 qualified 자산이 저장되더라도
탐색에 적용되지 않는다는 안내를 추가했다. 용량을 임의로 상향하지 않으며, 실제
다섯 bundle 크기를 확인한 뒤 저메모리/미확인 기기의 chunked admission 또는
명시적 비활성 상태 UX를 확정한다. 이 호환성은 v0.8.1 surface gate에서 `Open`이다.
Desktop GUI의 파일 기반 관리와 Discord compute 계층은 이 Web 변경으로 완료되지
않는다. 이후 Desktop에는 별도 Tauri native 관리 패널과 CLI 공통 저장소 호출이
연결됐지만, 서명된 실제 자산이 없어 설치·취소·탐색 효과를 제품에서 검증하지
못했다. 첫 Tauri Rust 타입 검사에서는 Windows UMCI가 생성된 build script를
차단했으나 수동 재시도에서는 빌드가 진행돼 비동기 `Send`/borrow 오류를 수정하고
`cargo check --locked --offline`을 통과했다.
pnpm injected `@clearra/ui`에서 형제 `ctk3/src` 상대 import를 찾지 못하던
Desktop Vite 빌드 문제는 Web/Desktop 공통으로 `@clearra/ui`의 선언된 export를
원본 workspace source에 결박하여 해결했다. 공개 export 전체의 해석 계약과
managed Desktop 실험용 Vite build를 통과했으나 Tauri 제품 실행은 검증되지 않았다.
따라서 Desktop을
`Qualified`나 `Released`로 표기하지 않는다. 실제 qualified Release asset의 브라우저 CORS/redirect 수신과
payload admission도 아직 제품 smoke가 없어 `Open`이다. 성능 ABBA는 사용자
지시에 따라 다음 단계로 미뤘으며, 이번 단계의 타입
검사나 Svelte 컴파일은 성능 채택 증거가 아니다.

native lifecycle은 catalog의 `active_session_shared_bytes`를 실제 parser가 계산한
공유 상주량의 서명된 상한으로 대조한다. allocator capacity가 toolchain마다 달라도
실제 값이 상한 이하면 허용하고, 상한을 넘으면 자산을 거절한다. 같은 process에서 같은 generation과 서명 statement가
이미 설치돼 있으면 payload를 다시 읽지 않고 단일 `Arc` owner를 재사용한다.
명시 삭제는 active session lease가 없을 때 in-process owner를 먼저 제거한 다음
persistent pointer를 지운다. 이 경계는
worker별 복제를 허가하거나 Web worker의 공유 소유권을 증명하지 않으며, 후자는 계속
`Open`이다. legal-board와 conditioned-reachability의 설치·삭제는 공통 mutation
경계에서 직렬화하여 동시 설치가 각각 상대 자산 부재를 관찰한 뒤 합산 128 MiB를
초과하는 경합을 금지한다. 명시 다운로드 저장소는 첫 쓰기 전에 기존 조상 경로의
link/reparse 여부를 검사하고, 전송·동기화·digest·구조 자격 중 어느 단계가 실패해도
staging 파일을 제거한다.

## 2. 릴리스 순서와 No-Go

1. v0.8.1에서 기존 solver 안정화, 다섯 profile의 exact legal-board,
   소형 조건부 도달성 BuildUp 가속기를 완료한다.
2. v0.8.1 exact SHA를 CLI, GUI, Discord 순으로 배포하고 P0/P1 회귀가 없음을
   대표 결과와 provider provenance로 readback한다.
3. v0.9.0을 accepted v0.8.1 위에 재기준화한다.
4. 전체 자격을 통과한 PC4 TB profile만 독립 활성화하여 v0.9.0을 배포한다.
5. 호환되는 upstream dataset generation만 v0.9.x에서 갱신한다.
6. 안정성과 운영 조건이 충족되면 v1.0.0으로 승격한다.

exact legal-board 또는 조건부 도달성 가속기 중 하나라도 정확성, 크기, 메모리,
성능 gate를 통과하지 못하면 v0.8.1은 **No-Go**다. 기능을 조용히 비활성화한
채 같은 완료 범위로 릴리스하지 않는다.

## 3. v0.8.1 필수 구현

### 3.1 다섯 profile exact legal-board

SRS, SRS+, SRS-X, Jstris 180, no-kick을 독립 생성·검증한다.

- `F_k`: 빈 필드에서 해당 profile의 정확한 규칙으로 `k`개를 놓아 도달 가능한 상태
- `R_k`: 남은 피스를 놓아 완성 필드까지 갈 수 있는 상태
- 제품 집합: `L_k = F_k ∩ R_k`

`reverse-layer-*`는 `R_k` 생성의 중간 증거일 뿐 제품 자산 또는 음성 판정
권위가 아니다. 생성기는 exact ILC, ordered first-success kick, rotation/180,
shape origin, 벽·바닥·상단, 좌표 원점 및 행 삭제 정규화 의미에 결박된다. 최종
`L_k`만 정렬·차분 압축한 immutable profile bundle로 게시한다.

생성기의 `L_k` 역추적은 이미 완결된 `L_(k+1)`에서 geometric predecessor를
checkpoint 가능한 run으로 열거·병합하고, 정렬된 `F_k`를 한 번 순차 읽어
교집합을 만든 뒤, 남은 `(source, piece)`만 exact ILC로 재검증한다. 이는
`F_k` 전체를 매번
정방향 확장하던 조건과 같은 집합 술어이며, spill identity는 target과 forward
layer digest를 모두 포함한다. 소스 변경과 소규모 동등성 테스트는 추가됐지만,
Windows UMCI 차단으로 변경 후 Rust 실행 검증 및 실제 다섯 profile 생성은 아직
`Open`이다. 이 변경을 실제 성능 채택 근거로 쓰지 않는다.

기존 reverse 산출물은 Git 이력에서 지우지 않지만 package, catalog 및 제품
loader에서 제외한다. 최종 binary는 Git source history가 아니라 GitHub Release
asset으로 게시하고, 저장소에는 schema/profile/rule identity, layer별 count·크기·
digest, 전체 chain identity와 URL이 들어간 작은 서명 catalog만 둔다.

exact negative는 empty-origin 10×4, 정확한 4L, 같은 qualified profile과 완결
generation에서만 허용한다. 초기 필드, 1~3L, 5~6L, 불완전 자산은
`OutOfScope`/`NotLoaded`/`NotQualified`로 기존 정확 경로를 계속한다.

하드 gate:

- 압축 최종 bundle 64 MiB/profile 이하, 다섯 profile 합계 320 MiB 이하
- 한 요청에서 선택 profile 하나만 load
- legal-board와 조건부 도달성 pack의 decoded/index 공유 peak 합계 128 MiB 이하
- worker별 전체 자산 복제 금지; host/shared read-only owner 하나만 허용
- 한도를 넘으면 shard 구조를 재설계하며 총량을 감추지 않음

제품 수명:

- CLI: `legal-board check|download|status|remove|generate --profile PROFILE`
- GUI: profile별 용량, 설치·검증 상태, 명시 다운로드·취소·삭제
- 동의 전 자동 다운로드 금지; 설치된 qualified profile은 eligible 요청에서 기본
  활성화하되 명시적으로 끌 수 있음
- Discord Gateway에는 적재하지 않고 승인된 immutable compute data layer에서 읽음
- 없음·손상·identity mismatch는 `Unknown`/`InvalidAsset`으로 fail-open

회귀 oracle은 Jstris 180 empty 4L P7P4의 456,459개와 SRS+ Full 4L P7P4의
456,923개 전체 결과다. 일반 SRS empty 4L P7P4의 438,791개도 사용자가
제공한 검증 기준으로 기록하고, 별도 exact solver 전체 집합 대조 전에는
qualification 증거로 승격하지 않는다. 세 개수 모두 조기 중단 조건으로 쓰지 않는다.

### 3.2 `BoardConditionedReachability`

exact legal-board와 별도 namespace·generation·loader를 가진 소형 조건부
도달성 계층을 구현한다. 질의 identity에는 다음을 모두 포함한다.

- compacted physical board와 원래 행 대응
- profile, piece, rotation, ordered kick와 180 의미
- 실제 도달한 entry pose 집합
- source/destination 및 앞선 kick 후보 전체의 충돌 dependency cell
- 벽·바닥·상단과 좌표 frame, 목표 lock 또는 질의 범위
- Boolean/witness/spin/finesse/CountAll 중 필요한 증거 수준
- generation과 relation schema identity

반환은 검증된 entry→exit/lock 관계, 합성 가능한 witness/macro, 완전성이 증명된
폐쇄 범위의 unreachable, 또는 `Unknown`/`OutOfScope`/`NotLoaded`/
`InvalidAsset` 중 하나다.

- first-success ordered kick을 보존한다.
- 장애물 추가로 뒤 kick이 열릴 수 있으므로 점유 단조 감소를 가정하지 않는다.
- 전역에서 실제 도달한 entry pose에만 국소 relation을 적용한다.
- 창 밖 우회, boundary 밖 footprint, dependency 누락 가능성이 있으면 fail-open한다.
- Boolean이 witness/spin/multiplicity를 보존하지 못하는 모드는 기존 exact 경로다.
- decision DAG, bounded entry/exit macro, bounded exact cache를 각각 비교한 뒤
  채택한다.
- solver hot loop에서 HTTP를 호출하지 않는다. host가 pack을 검증하고 session의
  immutable generation을 고정한다.
- 1~6L 및 초기 필드 요청도 검증 domain이면 사용할 수 있다. legal-board의 4L
  empty-origin 제한을 전파하지 않는다.

하드 gate는 16 MiB/profile, 다섯 profile 80 MiB, active-session 합산 공유 peak
128 MiB다. profile별로 독립 설치·교체하고 다운로드 전 실제 bytes를 표시한다.

### 3.3 선행 정확성 대장

| ID | 요구 | 현재 상태 |
| --- | --- | --- |
| LB-001 | 삭제 행 원위치와 bottom-prefix를 하나의 typed codec 계약으로 통합 | `Implemented`, `Locally validated`; 실제 profile exhaustive parity는 `Open` |
| LB-002 | `LoadedComplete`, `NotLoaded`, `OutOfScope`, `InvalidAsset`, verified absence 분리 | `Implemented`, `Locally validated`; 실제 손상/교체 제품 smoke는 `Open` |
| LB-003 | PC co-reachability와 일반 Build goal-independent reachability capability 분리 | `Implemented`, `Locally validated`; Build/Setup 전체 differential은 `Open` |
| LB-004 | profile 외 shape origin/start pose/boundary/lock/line-clear/coordinate 의미 fingerprint | `Implemented`, `Locally validated`; 실제 generation binding은 `Open` |

generation, manifest, payload, cache는 한 session에서 혼합하지 않는다. prune 후
자산 오류가 발견되면 그 작업을 기준 경로에서 재실행하거나 결과를 무효화한다.

### 3.4 기존 미완료 범위

다음은 계속 `Open`이다.

- PC·Build reducer와 CLI/GUI/Discord 결과 parity
- all solutions, build probability, minimum, score-minimals, replay, copy/page의
  동일 candidate universe
- 최종 exact SHA acceptance
- Pages·CLI·Discord 배포와 rollback readback

minimum first-canonical, lazy tie, work stealing, 수학 parity의 좁은 로컬 증거는
보존한다. 새 가속기가 candidate order나 reducer 의미에 닿으면 관련 검증을
다시 실행한다.

### 3.5 실행 순서

```text
입력 정규화와 면적·패리티 불변식
→ eligible 4L이면 exact legal-board negative filter
→ ILC/APDP Geometry 후보
→ CandidateProjection과 원래 행 대응
→ hard drop·기존 exact cache·조건부 도달성 relation
→ miss/out-of-scope이면 기존 exact reachability
→ BuildOrderGraph
→ supply·hold·pattern language
→ 공통 reducer
→ CLI/GUI/Discord presenter
```

두 가속기는 candidate를 정의하지 않고 기존 exact pipeline의 계산만 생략한다.

### 3.6 성능 채택 gate

동일 binary feature toggle로 각각 단독 A/B와 결합 ABBA를 실행한다.

- BuildUp p95 20% 이상 개선
- 전체 제품 p95 10% 이상 개선
- 작은 입력 p95 회귀 5% 이하
- result digest, candidate count, coverage, canonical order 완전 동일
- 1·2·11 worker 결과 동일
- worker별 복제 없이 peak 128 MiB 이하
- timeout/resource-limit 표본은 완료 시간으로 세지 않고 censored로 기록

결합 결과만으로 개별 가속기의 이득을 추론하지 않는다.

## 4. v0.9.0 PC4 Online Tablebase

### 4.1 권위와 출력 의미

v0.9.0은 V* 최선 수 추천 제품이 아니다. all-outgoing graph 또는 동일 완결성
manifest에 결박된 **전체 PC family**만 검색 권위로 사용한다. V*, policy,
Krylov state는 candidate 생성·prune·정렬·tie·확률·Setup 어디에서도 읽지 않는다.

graph traversal은 방문 edge와 출력 수에 비례하는 output-sensitive 실행이며 O(1)
전체 열거라고 부르지 않는다. edge는 concrete placement가 아니므로 Clearra ILC가
rotation, x/y, kick, line clear, replay witness를 materialize한다. 동일 source/target의
복수 legal placement는 기존 duplicate 계약대로 모두 보존한다.

### 4.2 profile 독립 활성화

SRS, SRS+, SRS-X, Jstris 180, no-kick을 별도 availability slot으로 관리한다.
profile 간 graph/index/KAT를 공유하지 않는다. 하나 이상의 profile이 전체 자격을
통과하면 v0.9.0을 부분 활성화할 수 있으며 나머지는 `not_qualified`로 남긴다.
v0.8.1 legal-board 자격은 TB profile 자격을 대신하지 않는다.

### 4.3 이관된 Open 대장

| 영역 | 남은 완료 조건 | 상태 |
| --- | --- | --- |
| Profile | format/index binding, provenance, license, KAT | `Open` |
| Target | profile·target별 1~4L terminal 및 graph completeness | `Open` |
| Generation | 서명 production manifest와 rollout pointer | `Open` |
| Materializer | profile별 upstream KAT와 offline exhaustive parity | `Open` |
| Jstris 전체 집합 | TB/offline 456,459 identity sequence 전체 일치 | `Open` |
| 메모리 | packed partial identity, survivor mask, 64-task wake window 후 P7P4 완주 | `Open` |
| 대형 실행 | first page/full export 시간, peak, logical/physical read, bridge 비용 | `Open` |
| Transport | 실제 WAN HTTP/2 tail, Range 수·bytes, 로컬 전체 다운로드 | `Open` |
| Surface | CLI/Web/Desktop/Discord qualified-profile smoke | `Open` |
| 오류 UX | CLI 명시 fallback은 `Implemented`, `Locally validated`; GUI/Discord 선택 UX와 실제 WAN 오류 대조 | `Open` |
| Discord | TB hit당 heavy Cloud Run invocation 0 | `Open` |
| Setup | profile별 completeness, offline differential 및 실제 A/B | `Open` |
| Migration | qualified generation/rollback 뒤 old static beta 제거 | `Open` |
| Release | exact app SHA + generation acceptance/deploy/rollback readback | `Open` |

indexed path, all-shard adjacency, outside-boundary dead proof, offline 456,459 family
receipt는 좁은 증거로 보존한다. 최종 identity sequence가 같기 전에는 target
qualification 또는 release 완료로 확대하지 않는다.

### 4.4 generation과 조회

- mutable HF ref는 discovery 전용이다.
- 실행은 resolved revision, profile files, length, format/index binding, digest,
  KAT version이 든 immutable generation에 고정한다.
- digest는 코드에 영구 고정하지 않고 generation manifest에 기록한다.
- 기본 온라인 조회는 정확한 Range만 허용하고 전체 파일 `200`은 거절한다.
- 명시 로컬 다운로드만 `tablebase check|download|status|remove --profile PROFILE`
  경로에서 허용한다.
- Jstris 180 약 661 MiB는 관측값이며 상수가 아니다.
- 로컬 파일과 HTTP Range는 같은 parser/materializer/reducer를 사용한다.
- CLI는 `--offline-fallback` 없이 heavy offline exact search를 시작하지 않는다.
- CLI의 `--offline-fallback`은 `--tablebase`와 함께만 유효하며, lookup 실패 때
  tablebase 표시만 제거한 동일 typed 요청을 기존 exact executor로 보낸다. 사용자가
  취소한 tablebase 요청은 fallback으로 재시작하지 않는다.
- GUI와 Discord도 사용자가 선택하기 전에 heavy fallback을 시작하지 않는다.

### 4.5 Setup 경계

TB는 complete PC candidate family만 공급한다. 기존 Setup engine이 shape, spin,
score, BuildUp, probability를 평가한다. PC Search와 SetupSearch 자격을 분리하고,
동일 query의 offline/TB digest와 candidate·coverage count가 같을 때만 Setup
acceleration을 활성화한다. offline이 완료되지 않고 TB만 완료한 경우
`feasibility dominance`로 기록할 수 있으나 제품 활성화 증거는 아니다.

### 4.6 v0.9.x와 v1.0.0

호환 generation 갱신은 발견 → candidate manifest → format/index/provenance/license
검증 → KAT/offline differential → Range/materialization A/B → canary → production
pointer 원자 promote → 이전 N개 generation 보존 순서다. schema, rule,
completeness 또는 공개 결과 의미가 바뀌면 data-only patch가 아니다.

n-PC 확률, Krylov 및 V* recommendation은 v0.9.x/v1.0.0 선행조건이 아니다.
관련 port/type은 feature-off, 무 I/O, 공개 surface 비노출 상태로만 유지한다.

## 5. 공용 provider·실패 계약

| 제품 | 권위 | namespace | 음성 판정 |
| --- | --- | --- | --- |
| exact legal-board | qualified `F∩R`, 4L domain | profile별 legal-board generation | 완결 domain에서만 |
| 조건부 도달성 | 검증된 국소 entry/exit relation | relation pack generation | 폐쇄 국소 domain에서만 |
| PC4 Tablebase | qualified all-outgoing graph | TB Range/local generation | miss 자체는 UNSAT 아님 |

공통 typed 상태는 `Ready`, `NotLoaded`, `OutOfScope`, `NotQualified`, `Miss`,
`InvalidAsset`, `SnapshotMismatch`, `RateLimited`, `Offline`, `Cancelled`다.
UI 기본 설명은 내부 hash/raw candidate ID를 숨기고 진단 export에만 provider,
profile, generation, completeness scope를 남긴다. 모든 provider는 동일 canonical
candidate stream과 기존 reducer로 진입하며 별도 minimum/score/probability 의미를
만들지 않는다.

## 6. 검증, CI, 배포, rollback

### 6.1 정확성

- 삭제 행 전체 조합, codec round-trip, 다중 삭제와 replay 원좌표
- ordered kick 앞 후보 성공/실패/경계/180 및 장애물 추가 비단조 사례
- 같은 내부 점유와 다른 외부 통로/entry pose, 창 밖 우회 후 재진입
- Boolean/witness/spin/finesse/CountAll 의미 분리
- profile별 legal-board exhaustive differential와 no-false-negative KAT
- relation 대 독립 primitive reference bounded exhaustive
- corrupt/truncated/wrong-profile/wrong-generation/stale-cache fail-open
- graph path 대 placement/replay offline parity
- fixed queue/hold/pattern/bag denominator/reducer parity
- Setup candidate/coverage/result digest parity

### 6.2 크기·자원·성능

profile별 compressed/decoded 크기, 공유/복제량, cold Range 수·bytes, session 수신량,
persistent cache/eviction, reader lease를 측정한다. 기존 solver, legal-board,
relation, 결합 후보를 ABBA하고 CandidateProjection/reachability/BuildOrderGraph/
language/reducer/render를 분리한다. 1~6L, 초기 필드, fixed queue, 큰 pattern,
build probability, minimum, replay를 포함하며 4L P7P4의 first page와 full export를
별도로 기록한다. profiling과 사용자 wall time 실행을 분리한다.

### 6.3 CI·배포

변경 owner 집중 테스트 → bounded exhaustive/fuzz/differential → Core/App/CLI →
Web/Desktop/Discord parity → exact source build-once artifact 재사용 → 배포 직전
canonical acceptance 1회 → Pages/CLI candidate smoke → Discord light/heavy 및 Oracle
path proof → publish/promote 결정 1회 → 모든 surface readback → tag/immutable release
순서다.

### 6.4 rollback

- v0.8.1: v0.8.0 application과 명령 registry로 전체 rollback
- v0.9.0: v0.8.1 application과 이전 static beta 상태로 전체 rollback
- v0.9.x: generation pointer를 이전 qualified generation으로 우선 rollback
- parser 비호환: application과 manifest 원자 rollback
- in-flight session: 시작 generation으로 완료하거나 typed cancellation
- health endpoint만이 아니라 CLI/Pages/Discord 대표 결과와 provenance readback

## 7. 최종 완료 판정

v0.8.1은 다섯 profile의 legal-board와 relation pack이 모두 `Qualified`, 모든
크기·메모리·성능 gate 통과, PC/Build reducer 및 세 surface parity, exact SHA
acceptance/deploy/rollback readback까지 끝나야 `Released`다.

v0.9.0은 최소 한 profile의 graph/target/materializer 전체 자격, signed production
generation, offline complete family parity, 실제 adapter와 fallback UX, Discord TB
hit의 heavy Cloud Run 0, provider 무음 혼용 없음, exact app SHA+generation acceptance와
rollback을 요구한다.

새 문서, generator 존재, reverse layer, synthetic CI 또는 한 번의 benchmark만으로
어떤 항목도 자동 완료하지 않는다.
