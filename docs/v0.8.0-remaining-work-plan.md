# Clearra v0.8.0 남은 작업 통합 구현·활성화·배포 계획

## 1. 권위, 범위, 시작 ledger

이 문서는 v0.8.0에 남은 작업의 단일 실행 계획이다. 저장소 루트의
`Clearra 핸드오프.md`가 더 높은 설계 권위이며, 이 문서는 그 문서를
변경하거나 구현 완료 증거로 대체하지 않는다. 충돌 시 권위 순서는
`Clearra 핸드오프.md` → 기능별 규범 문서 → contract fixture/test → source →
pinned 외부 비교 자료다.

구현 시작 기준은 2026-08-27의 `codex/v0.8.0`,
`042ec210d206ba81e2eccbe70ca629c3bef22d0d`이다. 시작 worktree의 기존 dirty
변경은 모두 보존한다. 최종 acceptance는 별도의 clean release worktree에서
exact candidate SHA로 수행한다.

계약 정규화 직후 구현 시작 ledger는 정확히 다음 49 blockers다.

| 구분 | 열린 행 |
| --- | ---: |
| capability | 38 |
| result-affecting option | 4 |
| accepted requirement | 7 |
| 합계 | **49** |

기존 46개에 다음 변경을 적용한다.

- 이미지 인식 `utility.to-fumen` capability와 implementation/planned projection을
  삭제하고 `sfinder-man/tofumen`, `sfinder-man/calibrate`를 명시적 exclusion으로
  재분류한다. native typed Fumen codec/output은 삭제 대상이 아니다.
- `pc.minimals`를 모든 exact optimal portfolio 열거가 빠진 `partial`로 다시 연다.
- `pc.score`를 score selection/order에서 attack을 완전히 제거하기 위한
  `partial`로 다시 연다.
- `artifact.solution-set`을 native CLI CTK3/Fumen 지원을 위한 `partial`로 다시 연다.
- `REQ-V080-021`을 `partial`로 추가한다.
- `pc.allspin-sol`은 구현 완료를 유지한다. 보존된 B2B-preserving solution
  family는 정상 목록이며 portfolio tie가 아니다. Discord의 canonical witness
  projection만 단일 결과다.

49개는 착수점 회귀를 재현하기 위한 역사적 ledger다. 그 뒤의 중간
`46 blockers` 및 capability별 진행 기록도 당시의 구현 순서를 설명하는
역사적 기록이며 현재 잔여량으로 해석하지 않는다.

## 1.1 최종 current-source reconciliation (2026-08-28)

현재 machine-readable registry의 잔여 ledger는 정확히 다음과 같다.

| 구분 | 열린 행 | 남은 경계 |
| --- | ---: | --- |
| capability | **0** | 없음 |
| result-affecting option | **0** | 없음 |
| accepted requirement | **0** | 없음 |
| 합계 | **0** | exact-source `release-freeze` snapshot까지 기록 완료 |

기능 구현은 PC, Build, Setup, Forward, Spin structure, Utility, profile,
solution artifact, durable delegation, Native/WASM/Web/Desktop/Discord 수직 경계를
포함해 current source에서 완료됐다. Exact portfolio alternative만 GUI에서
결과에 귀속된 paging과 명시적 CLI snapshot/continue로 전부 탐색하며, 정상
solution family에는 tie metadata가 없다. 모든 score selection, membership,
tie, canonical order는 score-only이고 attack은 동점 trace의 informational
관찰값일 뿐이다. Discord는 가장 작은 canonical candidate ID와 첫 canonical
portfolio 하나만 반환하고 tie 기능을 노출하지 않는다.

따라서 남은 작업은 새 기능 설계가 아니라 다음 하나의 직렬 release 흐름이다.

1. 최종 source bytes로 Web/WASM/Desktop build와 전체 gate를 다시 통과한다.
2. 완료: `audit-upstream-drift.mjs --phase release-freeze`의 별도 tracked
   fixture가 `no-drift`를 기록했고, 첫 canonical metadata 조립에서 발견한 CTK3
   build 누락을 고친 뒤 `retry1`, Node 22 타이머 fixture와 clean-checkout
   architecture authority를 고친 뒤 `retry2`, unsafe syntax 검출 강화와 실제
   UI WASM scan 경로를 닫은 뒤 `retry3`, canonical Windows runner에서 발견한
   host CPU 수 의존 worker assertion을 요청값/유효 범위 계약으로 고친 뒤
   `retry4`, native debug 실행 파일의 1 MiB main-thread stack overflow를 드러낸
   Windows ProductE2E 뒤 CLI가 16 MiB product-execution stack을 직접 소유하도록
   고친 `retry5`, zero-traffic Managed Secret smoke가 드러낸 Cloud Run worker
   ceiling과 shared execution-resource lease의 분리 권위를 하나의 명시적
   8-vCPU authority로 통합한 `retry6`, 배포 전 감사에서 발견한 만료된 Pages
   artifact와 current-main-only workflow 사이의 rollback 공백을 active source
   identity 검증, 공식 Pages `artifact.tar`의 90일 보존, REST artifact digest와
   inner-tar SHA-256 이중 결합, 배포 전/직전 package 재다운로드, digest-bound
   restore와 배포 직전 readback으로 닫은 `retry7`, 실제 다중해법 smoke의 audit와
   score coordinator 결함, bounded tie paging/Discord canonical 투영, v0.7.5 Pages
   노출과 CTK Render 통합, exact-SHA single-full-gate 배포 구조를 고정한 `retry8`,
   main-branch 전체 workflow-attempt 이력에 대한 exact-one acceptance, accepted
   product byte 재검증, accepted CTK3 v2, immutable Cloud managed-Secret Job smoke,
   typed Oracle 관찰, hash-bound 20분 관찰과 최종 journal 경계를 고정한 `retry9`,
   Desktop next-page prefetch의 AbortSignal을 실제 Tauri release/cancel token까지
   연결하고 완료 뒤 listener 제거를 고정한 `retry10`, metadata evidence 검증의
   import closure에서 Discord runtime/CTK3를 분리해 설치나 build 없이 실행되게 한
   `retry11`, runtime-backed product registry 계약을 이미 한 번 설치한 Discord
   job으로 옮기고 metadata에는 dependency-free upstream audit만 남긴 `retry12`,
   그 단일 owner와 metadata 금지를 release identity 정적 gate에 고정한 `retry13`,
   canonical Linux checkout에서 드러난 대형 product pager 계약 테스트의 영구
   behavior-level 단일 변경 이유를 명시한 `retry14`, 마지막 배포 권위 감사에서
   Pages/Discord/Oracle의 실제 producer readback과 3-stage atomic journal,
   completed-success publication finalizer를 닫은 `retry15`, 첫 실제 Cloud
   candidate preflight에서 발견한 Windows `gcloud.cmd` 실행 경계를 닫은
   `retry16` exact-candidate fixture도 각각 새 경로에 기록했다. 이 수정들은 capability
   inventory를 확장하지 않는다.
   `REQ-V080-020`과 `release_readiness`는 각각 `implemented`, `ready`이며 registry
   계약은 canonical acceptance에서 다시 검증한다.
3. 모든 non-secret source, test, 문서, Oracle v0.8 freeze/stage 및 final-source
   journal machinery를 하나의 clean candidate commit으로 고정하고, drift가 없는
   `main`에 fast-forward한다.
4. Exact main SHA의 canonical GitHub acceptance를 통과시킨다. 현재 active Pages
   source를 `.github/workflows/pages-rollback.yml`의 `capture` mode로 다시 build해
   공식 Pages `artifact.tar`를 90일 artifact로 보존하고 live identity, capture
   run/attempt, artifact ID/name/REST digest와 inner-tar SHA-256을 exact current-main
   rollback bracket에 결합한다. 정상 Pages workflow는 그 package를 build 전과
   deploy 직전에 각각 새로 내려받아 전체 tar 및 typed identity를 검증한 뒤에만
   deployment를 수행한다. 실제 run/deployment/artifact identity를 외부 attempt
   journal에 기록한다.
5. 같은 exact source archive로 Oracle candidate를 freeze/inactive-stage하고,
   Cloud image를 immutable digest로 build한 뒤 rollback authority를 먼저
   capture한다. Tagged zero-traffic candidate의 health, resource shape, managed
   Secret authenticated job smoke와 Oracle candidate proof를 통과시킨 뒤에만
   traffic을 100% 전환한다.
6. 같은 source에서 Discord command catalog를 동기화하고 exact readback digest를
   기록한다. 그 뒤 stable/tagged runtime, Oracle, representative operation,
   revision/traffic, command catalog를 실제 20분 이상 관찰한다.
7. 관찰이 통과하고 `main`이 그대로일 때만 annotated `v0.8.0` tag를 push한다.
   Tag-triggered workflow가 정확한 세 asset을 가진 immutable GitHub Release를
   게시한 뒤 실제 hash/size를 기록한다.
8. 외부 hash-chain journal을 `clearra.final-source-revalidation.v1` manifest로
   materialize하고 validator를 통과시킨 뒤에만 v0.8.0 완료를 선언한다.

Registry는 이제 blocker 0과 `ready`를 기록한다. Release-freeze fixture가
생성된 뒤에는 기능 source를 다시 바꾸지 않는다. 이후 어떤 exact-source
gate가 실패해 기능 source 변경이 필요하면 새 candidate SHA와 새 freeze
fixture로 다시 시작한다.

## 1.2 배포 재개 수정 ledger와 최종 단일 실행선 (2026-08-29)

1.1의 source-complete 판정 뒤 `e8f7e1538e21ca435304cb0b8955a19b1a2bc04f`
배포 전 실제 다중해법 smoke에서 두 제품 결함이 발견됐다. `pc.minimals`의
solution-set audit가 canonical selected key만 원본 universe로 잘못 사용했고,
`pc.score-minimals`는 score-only B안 postprocess 전에 Core의 coverage-only
minimum-cover 축약을 받아 exact scoring candidate identity를 잃었다. 따라서
`e8f7e153...` acceptance와 artifact는 새 release authority로 재사용하지 않는다.
Pages는 공개 결함 노출 직후 정확한 v0.7.5 source
`042ec210d206ba81e2eccbe70ca629c3bef22d0d`로 복구했고, Oracle activation,
Cloud Run traffic 전환, Discord command sync는 수행하지 않았다.

이 재개 wave는 기존 0-blocker 제품 범위를 다시 열어 확장하는 작업이 아니라,
아래 배포 차단 결함과 운영 구조를 같은 새 exact source에 고정하는 최종 wave다.

| 경계 | 새 exact-source 계약 |
| --- | --- |
| `pc.minimals` | audit pass-2 universe는 전체 normalized coverage source rows에서 재구성하고 public canonical selected keys는 그대로 1개 cover로 유지한다. IIOOO fixture의 네 single-member optimal portfolio를 `[1]`, `[2]`, `[3]`, `[4]` 순서로 모두 탐색한다. |
| `pc.score-minimals` | Core는 score 요청에서 generic coverage-only minimum-cover를 수행하지 않는다. 전체 candidate/coverage/exact-scoring graph evidence를 complete로 유지하고 score-only B안 portfolio 선택만 App coordinator에 위임한다. Attack은 equality, eligibility, membership, order에 관여하지 않는다. |
| GUI paging | portfolio page는 현재와 인접 page만, member page는 현재와 인접 page만 bounded하게 보존한다. Sliding window 뒤에도 typed `alternative_index`를 loader/cache identity로 사용해 모든 forward page를 순서대로 확인한다. |
| Discord | tie surface를 노출하지 않고 positive canonical candidate ID 중 가장 작은 첫 candidate 하나만 기존 응답 형식으로 투영한다. |
| Pages UI | 상단 탭과 모바일 선택기는 v0.7.5의 PC, Setup, Build probability, Damage, Spin Finder, CTK, Player 정확히 7개만 노출한다. 고급 `?tool=` route와 구현은 삭제하지 않는다. Render는 CTK 안의 current-page PNG/full-document GIF로 합친다. |
| Local-only GUI | 계산은 browser module Worker와 local WASM에서 수행한다. 네트워크는 `self.location.origin`의 배포된 WASM manifest/bindings/binary 및 tablebase 같은 digest-verified 정적 artifact 로드로만 한정하며 remote compute/API endpoint는 두지 않는다. |
| Test/deploy | 작은 변경은 changed crate/workspace/helper의 focused test만 실행한다. 전체 `ReleaseAcceptance`와 세 제품 build/smoke는 frozen exact-main SHA의 canonical `workflow_dispatch`에서 한 번만 실행한다. Tag run은 그 successful run ID와 artifact를 재검증·재사용하며 test/build를 반복하지 않는다. |
| Canonical acceptance | `main`, exact head SHA, `workflow_dispatch`의 모든 historical run attempt를 열거해 역사상 성공이 정확히 하나인지 검증한다. 성공 authority는 fresh attempt 1만 허용하며 rerun으로 가려진 성공이나 두 번째 성공은 모두 fail-closed 처리한다. Tag publish는 그 run/attempt에서 받은 세 product의 이름, 크기, SHA-256을 sealed evidence와 다시 비교한다. |
| Deployment evidence | CTK3는 canonical run에서 한 번 build/test한 accepted v2 artifact만 Oracle/Discord/acceptance가 공유한다. Pages capture/forward/restore는 run-attempt-bound sealed report를 통해 실제 artifact와 deployment/API/public readback을 결합한다. Discord sync는 accepted run, CTK3 manifest, canonical acceptance, catalog raw bytes와 independent readback을 하나의 authority로 묶는다. Cloud는 immutable `image@sha256`, numeric managed Secret version, zero-traffic revision과 execution-bound ephemeral Job smoke를 봉인하고, Oracle은 typed SSH owner boundary의 rollback capture와 read-only observation을 canonical durable file로 남긴다. 네 surface는 adapter hash를 고정해 1,200초 관찰하고 acceptance/deployment/publication stage를 atomic final journal에 결합한다. |

이 wave의 최종 upstream freeze는
`tests/fixtures/contracts/upstream_drift_release_freeze_retry16.v1.json`이며,
세 upstream의 pinned HEAD/source/command inventory가 모두 그대로인
`phase=release-freeze`, `status=no-drift` 증거다.

이 문서의 이전 문구 중 local full gate 뒤 같은 SHA의 GitHub full gate를 다시
요구하는 부분은 이 절과 7절의 single-full-gate 규칙으로 대체한다. 최종 남은
직렬 실행선은 다음과 같다.

1. 위 변경의 focused Rust/UI/Web/Discord/release-workflow 회귀를 통과시키고
   최종 Rust source fingerprint로 WASM/Pages build를 정확히 한 번 갱신한다.
2. 모든 tracked 변경을 새 clean candidate commit으로 만들고 drift 없는 `main`에
   fast-forward한다.
3. Exact main SHA의 `Publish Product Release`를 `workflow_dispatch`로 한 번 실행해
   전체 `ReleaseAcceptance`, Linux/Windows/GUI build와 packaged smoke를 통과하고
   accepted run ID 및 artifact hashes를 고정한다.
4. Active v0.7.5 Pages rollback authority를 새 main bracket에서 capture한 뒤 새
   Pages를 배포하고 v0.7.5 수준 navigation, CTK Render, IIOOO minimals 및
   score-minimals paging을 production smoke한다.
5. 같은 exact source archive로 Oracle freeze/inactive-stage와 Cloud tagged
   zero-traffic candidate/rollback/smoke를 완료한다. Discord의 실제 command sync나
   공개 메시지 전송처럼 외부 사용자를 향한 행위는 그 즉시 필요한 action-time
   확인 뒤에만 수행한다.
6. Oracle과 Cloud traffic을 활성화하고 Discord catalog를 readback한 뒤 exact
   identity를 최소 20분 관찰한다.
7. 관찰이 통과하고 main이 그대로일 때 annotated `v0.8.0` tag를 push한다. Tag
   workflow는 accepted run artifact 세 개를 cross-run download해 immutable release만
   게시하고, 아직 실행 중인 그 exact tag run에서 publication receipt를 90일 artifact로
   봉인한다. 원 tag run이 `completed/success`가 된 뒤 별도 `workflow_run` finalizer가
   수동 artifact ID/digest 없이 expected receipt artifact를 exact-one resolve하고 API
   digest, ZIP 구조와 canonical raw bytes를 검증해 final publication evidence를 만든다.
   로컬 resolver는 성공한 finalizer run/attempt의 artifact를 전역 목록에서 exact-one
   선택해 receipt와 evidence 원본을 다시 내려받고
   `clearra.release-publication-final-authority.v1`으로 봉인한다. 마지막으로 이 세
   publication 원본을 publication stage에 append하고 외부 attempt journal을
   materialize/validate한다.

## 2. `REQ-V080-021`: exact portfolio alternatives

### 2.1 적용 단위와 대표 metric

Tie는 여러 하위 해법으로 이루어진 서로 대체 가능한 최적 **portfolio**가
여러 개일 때만 성립한다. 내부 replay, trace, cell, contribution의 동률이나
정상 witness/solution family는 public portfolio tie가 아니다.

Probability나 score 같은 선행 조건으로 eligible set을 고정한 뒤 모든
portfolio form의 최종 대표 metric은 정수 member cardinality 하나다.

| Capability/form | 선행 eligible set | tie metric |
| --- | --- | --- |
| `pc.minimals/min-cover` | exact PC coverage universe | 최소 member 수 |
| `pc.score-minimals/max-score-cover` | pattern별 score-only 최고 candidate rows | 최소 member 수 |
| `build.cover`, `build.congruent-cover`, `build.setup-cover`, `build.evaluate.minimals`의 `min-cover` | exact reachable coverage universe | 최소 member 수 |
| 같은 Build capability의 `max-probability-minimum` | exact union probability의 전역 최댓값 | 최소 member 수 |
| `build.setup-cover-score`, `build.evaluate.score`의 `max-score-cover` | pattern별 score-only 최고 candidate rows | 최소 member 수 |
| `spin-structure.cover/min-cover` | profile/line predicate를 통과한 structure coverage universe | 최소 member 수 |

`max-probability-minimum`은 다음 단계식 의미다.

```text
p* = max exact_union_probability(P)
k* = min |P| such that exact_union_probability(P) = p*
ties = every canonical P satisfying probability(P) = p* and |P| = k*
```

`pc.score-minimals`와 모든 `max-score-cover` form은 B안으로 고정한다.

1. 각 `(candidate, pattern)`의 모든 legal replay 중 최대 정수 score를 구한다.
2. 같은 최대 score replay를 attack으로 줄이지 않는다.
3. 각 pattern의 전역 최고 score를 달성한 모든 candidate를 eligible하게 남긴다.
4. candidate row는 그 candidate가 최고 score를 달성하는 pattern 집합이다.
5. 그 row들로 exact minimum-cardinality cover를 구한다.
6. 모든 equal-cardinality optimal portfolio를 보존한다.

Score equality, eligibility, dominance, ranking, portfolio membership,
canonical order, Discord representative selection에 attack을 사용하지 않는다.
표시용 attack이 필요하면 normalized trace key가 가장 작은 equal-score trace를
대표로 선택하고 `informational_attack_basis=canonical_equal_score_trace`를
명시한다. Score는 exact integer로 비교하며 epsilon이나 `f64` ranking을
사용하지 않는다.

### 2.2 Identity, enumeration, snapshot

공통 계약은 `portfolio-alternative-set.v1`,
`portfolio-alternative-page.v1`, `portfolio-member-page.v1`,
`portfolio-snapshot.v1`로 나눈다.

- candidate ID는 stable normalized solution key를 정렬한 뒤 1부터 부여한다.
- portfolio identity는 오름차순 numeric candidate-ID vector다.
- portfolio 순서는 그 vector의 숫자식 lexicographic order다.
- 기본 단일 결과와 Discord 결과는 첫 portfolio다.
- worker 수, backend, 탐색 순서, hash bucket, 입력 row 순서는 결과를 바꾸지
  못한다.
- set에는 query/source/profile/universe/build identity, candidate-map digest,
  검증된 최적 cardinality, `known_alternative_count_decimal`, nullable
  `total_alternative_count_decimal`, `enumeration_complete`, incomplete reason과
  restart 정보가 포함된다.
- 외부 page는 portfolio 하나를 담고, member page는 result-bound candidate
  dictionary를 100개씩 참조한다.

Enumeration은 semantic top-K 제한 없이 lazy하고 exact해야 한다.

1. 첫 pass에서 정확한 최적값 `k*`와 lexicographically smallest canonical
   portfolio 하나를 증명한다.
2. 두 번째 restartable exact pass가 원본 row identity에서 모든 `|P|=k*`
   portfolio를 열거한다.
3. 최적값 탐색용 dominated/equal-row 축약은 허용하지만 두 번째 pass는 제거된
   equal candidate identities를 복원한다.
4. equal-depth branch를 자르는 기존 single-winner pruning을 all-optima
   enumerator에 사용하지 않는다.
5. 모든 결과를 선물질화하지 않는다. `known_count`는 단조 증가하고 exact
   `total`은 traversal seal 뒤에만 채운다.
6. timeout, cancellation, resource exhaustion, snapshot failure는
   `enumeration_complete=false`와 exact restart 정보를 반환한다. 일부 결과를
   전체라고 표시하지 않는다.
7. generic minimum-cover의 greedy fallback은 product exact authority가 아니다.

### 2.3 정상 family/list는 tie가 아니다

다음 결과는 각 capability의 정상 count/order/paging 계약을 사용하고 tie
metadata를 붙이지 않는다.

- `pc.score`, `pc.best-save`, `pc.score-finder`, `pc.allspin-sol`
- Setup 네 ranked family
- `forward.damage`, `forward.spin`, `forward.ren`
- `spin-structure.search`, `spin-structure.guaranteed`
- `utility.sequence`, `utility.sequence-dependencies`

### 2.4 GUI, CLI, Discord

Main Web workspace, `/pc-solver`, Tauri/Desktop는 result와 함께 portfolio store를
자동 준비한다. 외부 한 page에는 portfolio 하나, 내부 member page에는 100개를
표시한다. 사용자 navigation과 한 page 안전 prefetch에서만 enumerator를
진행한다. JS에 전체 alternative를 누적하거나 client-side 정렬하지 않는다.
현재/이전/다음 page 상태만 유지하며 새 검색, 취소, navigation, dispose, app
exit에서 handle을 해제한다. v0.8에서는 share URL이나 restart 복구를 제공하지
않는다.

CLI 기본 byte/schema 의미는 유지한다. 명시적 경로는 다음과 같다.

```text
clearra <original-command> ... --ties --tie-snapshot PATH
clearra continue --tie-snapshot PATH --tie-cursor TOKEN
```

Snapshot은 versioned append/checkpoint 형식이며 query/build/profile/candidate-map과
exclusive lock에 결합한다. Initial path는 새 파일이어야 하며 existing file,
symlink/reparse target, unsafe parent, concurrent writer를 거부한다. Stale,
tampered, 다른 query/build cursor와 I/O 오류는 typed failure다. 기존 positional
PC continuation token은 그대로 지원하고 parser에서 tie continuation과
fieldwise 구분한다.

Discord에는 tie option, count, explanation, cursor, paging, button, modal,
autocomplete, attachment를 추가하지 않는다. Direct와 remote job 모두 tie
flag를 전달하지 않고 canonical first portfolio 하나만 기존 형식으로
게시한다. Discord validator는 alternative array와 tie metadata를 거부한다.

## 3. Native CTK3/Fumen solution output

`artifact.solution-set`을 v2로 확장한다.

- 모든 solution-bearing native CLI command에서
  `--format text|json|ctk3|fumen`을 지원한다.
- `--solution-artifact-format compact|json|ctk3|fumen`을 지원한다.
- 기본 text/compact 동작은 바꾸지 않는다.
- Portfolio cursor가 있으면 현재 portfolio 전체를 출력한다. 화면의 member
  page 100개만 인코딩하지 않는다.
- 정상 family는 capability의 현재 family/page 계약을 따른다.
- 대규모 CTK3는 기존 segment/bundle 규격으로 streaming한다.
- Fumen은 호환 가능한 multi-page document만 생성하고 표현 불가능한 CTK3
  정보를 silent loss 없이 capability error로 거부한다.
- cancellation, codec, publication 실패를 부분 성공으로 보고하지 않는다.
- Native file output은 atomic-new-file, no-overwrite, symlink rejection을
  유지한다.

Native CLI는 Node/TypeScript subprocess나 network codec을 사용하지 않는다.
언어 중립 `docs/ctk3.md`와 shared conformance fixture가 wire authority다.
`packages/ctk3`는 browser/Discord TypeScript 구현을 유지하고 새 native Rust
codec은 같은 fixture의 canonical bytes, document identity, checksum,
page/color/comment/operation, segment/bundle 동작을 맞춘다. `clearra-output`은
codec을 복제하지 않고 typed writer dispatch만 소유한다.

## 4. Capability implementation

### 4.1 PC

- `pc.path`: legal replay/path evidence를 materialize하고 candidate, pattern,
  trace, hold transition, consumption, line-clear identity를 보존한다.
- `pc.minimals`: 기존 exact single canonical cover를 보존하면서 공통 all-optimal
  enumerator/store를 연결한다.
- `pc.score`: score-only selection/order로 좁게 수정하고 equal-score/different-
  attack candidates를 정상 family로 보존한다.
- `pc.score-minimals`: B안 `pc-score-portfolio.v2`를 완성하고 candidate,
  coverage, legal replay evidence를 분리 보존한다.
- `pc.saves`: terminal hold + active-bag remainder canonical multiset을 public
  save-group identity로 사용한다. 동일 `(group, pattern)`의 여러 trace는 pattern
  weight를 한 번만 더한다. 전체 universe probability와 conditional-given-PC를
  분리하고 bag provenance가 없는 fixed source는 fail closed한다.
- `pc.best-save`: `clearra-save-v1`의 lexicographic tuple
  `(T6/I4/O3/J1/L1/S0/Z0 weighted total, min(J,L), exact group probability)`을
  사용하며 모든 exact 동점 winner를 정상 목록으로 반환한다.
- `pc.score-finder`: fixed queue의 최고 정수 score witness를 모두 반환하며
  attack을 선택/정렬에 사용하지 않는다.
- `pc.allspin-sol`은 full preserving solution family 보존 characterization을
  유지하고 Discord에서만 canonical first witness를 고른다.

### 4.2 Build options와 capabilities

- `build.queue-knowledge`: `oracle|visible-7`, 기본 `oracle`. `visible-7`은
  `(current hold, visible next 7)` observation class 내에서 동일 action을 요구하는
  exact policy다. `tiling-only`와는 불가하다.
- `build.objective`: `all|unique|min-cover|max-probability-minimum|max-score-cover`.
  호환 alias는 `minimum-cover -> min-cover`만 유지한다. Fixed subcommand와 같은
  objective는 허용하고 충돌은 실행 전에 거부한다.
- `build.score-profile`: `tetrio|guideline|jstris-ultra`, 기본 `tetrio`.
  Score-capable form에서만 허용하고 모든 builtin은 현재
  `basic-approximation`, `profile_specific_exact=false`를 공개한다.
- `build.initial-b2b`: `0..65535`, 기본 0. Score-capable form에서만 허용하며
  coverage/probability union을 바꾸지 않는다.

12 Build capability는 공통 geometry, realization, BuildOrder, HoldAutomaton,
coverage evidence를 재사용한다. Probability는 candidate 확률 합이 아니라
PatternBitSet OR union으로 계산한다. Supplied-solution evaluate는 input identity와
replay legality를 검증한 뒤 같은 reducer를 사용한다. Partial universe, replay,
weight를 authoritative probability, score, winner로 승격하지 않는다.

### 4.3 Setup, Forward, Spin structure

- Setup primary rankings은 각각 exact Joint, Build, conditional PC,
  score-only다. 모든 동점 setup은 정상 family다. `setup-length`는 별도
  deterministic rule이며 `auto`는 all/build에서 longer, pc에서 shorter다.
- `forward.damage` maximum은 최고 damage의 모든 outcome/path를, at-least는
  predicate를 통과한 모든 결과를 반환한다.
- `forward.spin`은 ordered queue/hold search이며 public identity는 terminal spin
  outcome이다. Movement/kick trace 차이는 evidence로 접는다.
- `forward.ren`은 exact fixed queue, 최대 22 pieces, initial hold empty, hold on
  기본이다. 시작 board의 완성 행은 지우되 REN에 세지 않는다. 각 accepted
  lock은 줄을 지워야 하며 `ren_count=max(line_clear_count-1,0)`이다. 첫 piece가
  clear하지 못하면 no-solution이다. Maximum REN의 모든 distinct witness와
  multi-parent trace DAG를 보존한다.
- Spin structure는 unordered inventory/no-hold authority를 forward spin과 분리한다.
  Search와 guaranteed는 정상 structure family이고 cover만 exact portfolio tie다.

### 4.4 Utility, profiles, artifact

- `utility.parity`: global coordinate 기반 static observation만 반환하고
  `feasibility_claim=false`, `pruning_authority=none`으로 고정한다.
- `utility.to-gray`: occupancy color만 gray로 바꾸고 page/operation/comment/
  garbage/dimension identity를 보존한다.
- `utility.render`: bounded deterministic typed-document render만 수행한다.
- `utility.fumen`: combine/split/mirror/comment/operation transform을 typed
  document에서 수행하고 표현 불가능한 정보는 거부한다.
- `utility.sequence`: provided operation traces를 lossless normalize/replay-
  validate한다. Exact order 탐색은 하지 않는다.
- `utility.sequence-dependencies`: actual reachability 기반 exact
  `OperationOrderLanguage`를 구현한다. Queue/hold 없이 initial board, concrete
  operation multiset, rule/kick profile만 사용한다. Dynamic word span으로 최대
  4,096 operations, `dependency_long=900 seconds`, arbitrary-precision decimal
  count, universal precedence closure와 deterministic transitive reduction을
  지원한다. Incomplete graph에서 absolute dependency를 게시하지 않는다.
- `utility.mirror`: field/page/operation/rotation typed transform과 double-mirror
  identity를 검증한다.
- `request.profiles`: board/piece/bag/rule/spin/score profile을 request-local로
  선택하고 unsupported/unverified profile은 fallback 없이 거부한다.
- `artifact.solution-set`: native CTK3/Fumen v2와 current portfolio/family
  export를 연결한다.

## 5. Runtime/release requirements

### 5.1 `REQ-V080-013` durable delegation journal

Two-phase publication 순서는 `Prepared` durable ACK → non-executable offer →
worker accept ACK → `Accepted` durable ACK → `Published` durable ACK → executable
payload다. 상태는 `Prepared → Offered → Accepted → Published → Running/Renewed
→ ResultSealed → ResultApplied → Completed`이며 terminal은
`Revoked|Expired|Cancelled|FailedClosed`다.

기본 시간은 offer acceptance 10초, heartbeat 5초, persisted renewal 최대 30초,
active lease expiry 120초, terminal/dedupe tombstone 24시간이다. Corrupt,
unresolved, unknown-commit journal은 TTL 삭제하지 않고 quarantine한다.
Native journal은 per-job canonical UTF-8 JSON Lines, decimal u64, monotonic
sequence, prior/event SHA-256 chain, append+flush+`sync_all` 후 ACK를 사용한다.
Browser는 IndexedDB, tests는 injected clock/ACK failure journal을 사용한다.

### 5.2 Drift와 final-source evidence

- 구현 시작과 release freeze에 pinned upstream을 각각 audit한다.
- `REQ-V080-020`은 release-freeze snapshot 없이는 완료하지 않는다.
- `audit-upstream-drift.mjs --phase release-freeze --output PATH`는 현재 registry
  identity에 phase를 결합하고, link가 없는 안전한 parent의 새 파일에만 검증된
  snapshot을 `sync`한다. 기존 implementation-start snapshot의 역사적 날짜는
  바꾸지 않되, release-freeze snapshot은 exact candidate registry 날짜와 달라지면
  거부한다. 실제 freeze 실행 전에는 `REQ-V080-020`을 닫지 않는다.
- `clearra.final-source-revalidation.v1`에는 exact commit/tree SHA, clean 상태,
  registry/schema/toolchain/build identities, drift snapshots, canonical gate,
  native/WASM/Desktop/Discord reports, release artifact hashes, Pages identity,
  Discord image/job/Oracle identities, command catalog, rollback snapshot을 담는다.
- `scripts/release/validate-final-source-revalidation.mjs`는 위 필드를 exact
  closed schema로 검증하고 source identity가 하나라도 섞이거나 readiness가 0이
  아니면 fail closed한다. 임의의 `command_catalog_sha256` 또는
  `report_sha256` 문자열은 증거가 아니다. Final validation과 journal
  materialization은 `clearra.discord.command-catalog-sync.v1` producer report와
  `clearra.production-observation.v1` producer report 파일을 모두 직접 읽어
  canonical byte/hash/schema를 검증하고, Pages deployment, Discord application과
  catalog, Oracle release/tree/settings, Cloud revision/image/traffic identity를
  manifest와 fieldwise 대조해야 한다. 실제 candidate manifest와 배포 후 producer
  report가 생기기 전에는 이 기반만으로 release 완료를 주장하지 않는다.
- `apps/clearra-discord-bot/scripts/discord-command-catalog-release.mjs`는 같은
  source commit의 canonical writable command JSON과 SHA-256을 만든다. `sync`는
  Discord의 independent GET 결과를 mutation 전에 새 prior snapshot 파일로 먼저
  `fsync`하고 그 exact observed preimage에서 bulk overwrite를 최대 한 번 수행한 뒤
  independent readback digest를 seal한다. `restore`는 현재 live digest가 sync
  report의 exact post-sync digest와 같을 때만 prior snapshot을 복원하고 exact
  readback을 확인한다. Discord token은 오직 `DISCORD_TOKEN` 환경변수로 전달하며
  snapshot/report/manifest/stdout에 저장하지 않는다.
- `scripts/release/observe-production-surfaces.mjs`는 Discord catalog GET,
  Oracle SSH read-only ops proof, Cloud stable/tagged health와 revision/image/traffic/
  resource readback, Pages HTTP build identity를 하나의 source commit에 묶어 실제
  1,200초 이상 반복 관찰한다. Production CLI는 duration override를 제공하지
  않는다. `scripts/release/materialize-production-probe-spec.mjs`는 non-secret
  deployment authority에서 Discord/Cloud/Pages 공용 Node adapter와 Oracle
  PowerShell owner를 regular non-link file SHA-256으로 고정하며 catalog/sync/smoke
  producer 파일의 raw SHA-256도 spec argument에 묶는다. 매 probe 직전 adapter를
  다시 hash하고 canonical closed JSON만 수용한다. Oracle은 공개 health endpoint를
  가정하지 않으며 active release/tree/settings, pinned PID/READY, boot ID와
  monotonic process-start, strictly increasing fresh-operation/observation 시간을 SSH
  authority로 반환한다. Cloud candidate는
  `scripts/release/cloud/candidate-release-v080.mjs`의 `deploy`/`smoke` 두 경계만
  사용하여 source tag를 먼저 immutable `image@sha256`으로 해석하고 numeric Secret
  version을 service와 ephemeral managed-secret Job에 각각 주입한다. Sealed
  `clearra.cloud.candidate-smoke.v1`은 exact revision/tag/URL/image와 zero-traffic
  service/revision readback hash, smoke Job/execution identity, execution readback hash,
  bounded `/jobs` solution-set hash를 닫는다. Cloud observation은 같은
  revision/image/tag의 이 report를 요구한 뒤 post-cutover 100% traffic/resource와
  stable/tagged health를 독립적으로 다시 읽는다. Discord token과
  Oracle identity-file path/value는 runtime environment에만 있고 authority/spec/
  report에 들어가지 않는다. 테스트만 injected probe와 clock으로 짧은 duration을
  사용한다.
- `scripts/release/final-source-stage-evidence.mjs`는 실제 producer 파일을 직접 읽어
  source-bound `acceptance`, `deployment`, `publication` 세
  `clearra.final-source-stage-evidence.v1` report를 만든다. Acceptance는 exact clean
  source, canonical acceptance, registry/contracts와 마지막 release-freeze fixture를,
  deployment는 Pages capture/deployment, Discord catalog/prior/sync authority/report,
  Cloud smoke, Oracle capture/observation, production probe spec/observation을,
  publication은 completed-success publication evidence, 전역 resolver가 만든 final
  authority, 그 원 receipt bytes를 각각 fieldwise 검증한다. 임의 문자열 hash나
  손으로 만든 event payload는 권위가 아니다.
- `scripts/release/final-source-attempt-journal.mjs`는 source commit을 고정한 canonical
  JSONL hash chain에 위 세 stage만 정확히 `acceptance → deployment → publication`
  순서로 받는다. CLI는 `initialize`, `append-stage --stage-evidence PATH
  --stage-evidence-file-sha256 SHA`, `materialize` 세 명령만 허용하고 임의
  `append --kind --payload` 경로는 없다. `append-stage`는 stage 전체 event batch를
  새 파일에 쓰고 `fsync`한 뒤 journal을 원자 교체하므로 실패하면 기존 bytes가
  그대로 남는다. Exclusive writer lock, torn/tampered chain rejection, closed event
  cardinality와 source identity를 모두 재검증한다.
- Final materialization은 journal뿐 아니라 acceptance/deployment/publication stage
  evidence 세 파일과 각 raw file SHA-256을 다시 읽는다. 또한 canonical acceptance,
  Pages capture/deployment, Discord catalog/prior/sync authority/report, Cloud smoke,
  Oracle capture/observation, production probe spec/observation, publication
  receipt/evidence/final-authority 원본과 각 raw SHA-256을 모두 다시 열어 세 stage를
  재구성하고 journal의 event bytes와 정확히 비교한다. 그 뒤에만 library-only 최종
  validator를 내부 호출하고 새 manifest를 쓴다. Standalone validator CLI는 없으며
  직접 실행은 fail-closed한다. 누락된 배포, observation, tag 또는
  immutable-release event는 합성하지 않는다.
- v0.7.5 proof나 다른 SHA의 workflow success를 v0.8 authority로 재사용하지
  않는다.
- Registry의 `implementation_status`는 repository source가 해당 requirement의
  수집기, validator, fail-closed runbook과 회귀를 구현했는지를 기록한다.
  `REQ-V080-016`과 `REQ-V080-017`을 이 의미로 `implemented`로 닫는 것은 실제
  Pages/Discord/tag/release가 완료됐다는 주장이 아니다. Workflow metadata gate는
  배포 전에 readiness 0을 요구하므로 이 구분이 없으면 배포 후 registry 수정이
  새로운 source SHA를 만들어 exact-source release를 불가능하게 한다.
- 실제 release attempt journal과 materialized manifest는 source tree 밖의
  `%LOCALAPPDATA%\Clearra\reports\release-v0.8.0\<attempt-id>\`에만 저장한다.
  같은 source commit을 끝까지 결합하며, final manifest 검증 전에는 운영 완료를
  선언하지 않는다. 저장소에 배포 성공을 미리 기록하거나 post-deployment commit을
  만들어 증거 identity를 순환시키지 않는다.

## 6. 실행 순서

### Wave 0 — 계약 동결

- 이 계획, registry 49-blocker 시작점, `REQ-V080-021`, image recognition
  exclusion, portfolio matrix를 반영한다.
- Architecture, scoring, output 문서의 score-aware 비노출/attack/CTK3 경계를
  새 결정과 맞춘다.
- Structural registry test는 통과하고 readiness는 정확히 49 blockers로
  의도적으로 실패해야 한다.

### Wave 1 — 공유 기반

- Coverage/Objectives: exact optimum proof, all-optimal enumerator, snapshot,
  max-probability와 score-only reducers
- Score/Postprocess: attack comparator 제거, canonical equal-score trace,
  complete score evidence
- Document/Output: native CTK3 codec, CTK3/Fumen solution artifact v2
- Runtime: native/IndexedDB/test delegation journal과 fencing/recovery
- BuildOrder: reusable exact graph, dynamic operation bitset, big-integer count,
  lazy order enumeration

### Wave 2 — Capability verticals

PC와 Build를 먼저 연결하고 Setup, Forward, Spin-structure를 병렬 구현한다.
Utility/profile vertical은 shared BuildOrder와 codec 기반이 준비된 뒤 합친다.
Legacy alias는 별도 solver가 아니라 canonical typed request로만 lowering한다.

### Wave 3 — Surface integration

App request/result validator를 authority seam으로 삼아 CLI/WASM/Web/Desktop/
Discord가 core를 직접 우회하지 않게 한다. GUI portfolio viewer, CLI tie
snapshot, Discord canonical-only projection을 연결한다. Help/i18n/telemetry/
result allowlist는 해당 vertical의 end-to-end evidence가 생긴 뒤 활성화한다.

### Wave 4 — Ledger zero와 freeze

38 capabilities, 4 options, 7 requirements를 current-source 구현 evidence로
닫고 readiness 0을 만든다. REQ016/REQ017의 이 ledger 폐쇄는 release-evidence
machinery 완료를 뜻하며 실제 운영 release 완료는 post-deployment journal이
별도로 판정한다. Version과 v075 namespace를 v0.8.0/v080으로 바꾼다.
Candidate SHA 이후 기능 코드가 바뀌면 freeze와 전체 acceptance를 새 SHA에서
다시 실행한다.

## 7. Test and acceptance

다음 범위를 최소 acceptance로 삼는다.

- Registry: migration 직후 structural pass/readiness 49, 최종 readiness 0,
  exhaustive upstream disposition
- Portfolio: equal/dominated rows, identity 복원, all optimal covers,
  duplicate/no-omission, canonical order, progressive count, cancel/restart,
  stale/tampered/mismatched cursor
- Score: equal score/different attack 보존과 attack 불변 canonical result
- Save: terminal hold, duplicate multiset, bag epoch, pattern/group dedup,
  unconditional/conditional probability와 provenance rejection
- Build: objective allow/deny, oracle/visible-7 divergence, profile/B2B effects,
  initial-b2b bounds
- BuildOrder: independent factorial, chain, diamond, conjunction, line-clear
  remap, kick-sensitive dependency, converged parents, brute-force small oracle,
  word boundaries와 4,096 limit
- REN: initial full row, first clear/non-clear, multi-line, hold divergence,
  blocked/source-exhausted, distinct paths, 22/23 boundary
- Utility/codec: coordinate invariants, roundtrip, deleted image command absence,
  TypeScript/native CTK3 KAT, Fumen compatibility/rejection, streaming atomicity
- Runtime: every durable ACK crash point, torn/corrupt journal, IndexedDB failure,
  stale fencing, duplicate result
- Surface: GUI lifecycle/paging, CLI default golden/snapshot safety, Discord no-tie
  fields, normal-family preservation, native/WASM/Desktop/Discord identity
- Sanitizers, allocator accounting, cancellation, worker shutdown, store release

최종 frozen exact-main SHA의 canonical `workflow_dispatch`가 다음 gate를 정확히
한 번 실행한다.

```powershell
powershell -NoProfile -File scripts/clearra.ps1 -Task ReleaseAcceptance -ExecutionSurface Trusted
```

개발 중에는 위 command를 반복 실행하지 않고 `docs/test-policy.md`의 changed-path
focused scope만 실행한다. Canonical workflow의 gate는 NoProductDebt, Adversarial,
C sanitizers, Rust exact tests, Product
E2E, WASM, Web/Pages, Desktop, Discord, render/codec KAT, readiness 0,
release-freeze audit와 final-source evidence machinery를 한 source에서 증명해야
한다. 실제 Pages/Discord 배포, 20분 observation, tag, immutable release를 포함한
final-source manifest는 그 exact source가 gate를 통과한 뒤 외부 journal에서
완성·검증한다.

## 8. Main 반영, 배포, rollback

1. 모든 구현의 changed-path focused test를 통과하고 하나의 clean candidate
   commit으로 고정한다. 이 단계에서 전체 acceptance를 중복 실행하지 않는다.
2. `origin/main` drift가 없을 때만 exact candidate를 main에 direct
   fast-forward push한다. Force push는 금지한다.
3. Drift가 있으면 최신 main 위에서 비파괴 재적용하고 새 SHA에서 freeze와
   focused scope를 다시 실행한다.
4. Exact main SHA로 canonical `release-cli` workflow를 한 번 dispatch해 전체
   gate와 제품 build/smoke를 수행하고 accepted run ID 및 artifact hashes를
   manifest에 결합한다.
5. Discord no-traffic image-digest/Oracle candidate를 준비하고 smoke한다. 동시에
   active Pages source를 새 rollback workflow의 `capture` mode로 재빌드해 live
   source identity와 일치함을 검증하고, non-expired Actions artifact ID/digest를
   rollback snapshot에 결합한다.
6. rollback artifact가 exact current-main bracket에 묶였음을 readback한 뒤 Pages
   exact-SHA를 배포하고 identity/tie/family/CTK3/Fumen smoke를 통과시킨다.
7. Discord no-traffic health/auth/result/canonical-only smoke 뒤 Oracle activation,
   traffic 100%, production smoke를 수행한다.
8. Discord command catalog sync를 마지막 public mutation으로 수행한다. Sync
   직전 prior snapshot/digest와 sync 직후 independent readback/report를 tracked
   catalog producer로 보존한다. 이후 rollback은 current digest가 그 report의
   exact post-sync digest일 때만 prior snapshot restore/readback을 허용한다.
9. Hash-bound 네 probe adapter로 Discord/Oracle/Cloud/Pages producer report를
   1,200초 이상 생성·seal하고 final manifest identity와 대조한다. 이 observation을
   통과한 뒤 exact main SHA에 annotated `v0.8.0` tag와
   immutable release를 만든다. Asset은 Linux CLI, Windows CLI, Windows GUI
   정확히 세 개이며 이름은 각각 `Clearra-CLI-v0.8.0-linux-x86_64`,
   `Clearra-CLI-v0.8.0-windows-x86_64.exe`,
   `Clearra-GUI-v0.8.0-windows-x86_64.exe`다.

Tracked non-secret orchestrator는 attempt journal을
`%LOCALAPPDATA%\Clearra\reports\release-v0.8.0\<attempt-id>\`에 남긴다. Pages,
Discord image/traffic, Oracle revision, command catalog의 이전 상태를 배포 전에
보존한다. Pages rollback authority는 current live source SHA를 확인한 capture run,
`clearra-pages-rollback-<snapshot>-authority-<main>-run-<id>-attempt-<n>` artifact
ID/name/REST digest, 공식 Pages `artifact.tar`의 inner SHA-256, 최소 89일 retention과
non-expired readback으로 구성한다. Capture workflow는 package와 별도로
`clearra.pages.rollback-capture-authority.v1`을 만들고 run/attempt-bound 이름의 90일
report artifact로 올린다. Forward/restore는 snapshot SHA와 capture run ID만 입력받아
Actions API에서 이 report를 exact-one resolve하고, report에서 package artifact
identity를 파생한다. 사용자가 artifact ID/name/digest/tar hash를 다시 입력하는 경로는
없다. 두 workflow는 build 전과 public mutation 직전에 report와 package를 모두 새로
내려받아 검증한다. 실제 forward/restore 뒤에는 workflow run/attempt, upload-pages
artifact ID/name/API digest, Pages configuration, deployment status API와 public
identity readback을 봉인한 `clearra.pages.deployment-authority.v1`을 별도 90일
artifact로 남긴다. 이 artifact는 보존 기간이 길더라도 capture 당시의
exact authority-main bracket에만 유효하며 main drift를 포괄하는 일반 복구 권한으로
승격하지 않는다. Capture의 failed-job rerun은 새 run-attempt-bound artifact를 만들 수
있지만, forward와 restore public mutation은 고정 `github-pages` artifact의 attempt 간
모호성을 없애기 위해 rerun을 거부하고 항상 새 workflow dispatch를 요구한다. Restore는
같은 bracket과 capture run/job/artifact authorities를
다시 검증하고 fresh-downloaded tar의 header checksum, 경로, entry type, 중복,
typed identity와 WASM manifest를 확인한다. 또한 live candidate 전체 identity를 초기와
배포 직전에 다시 읽고 `ROLLBACK:<current>:TO:<snapshot>` sentinel 및 v0.8.0 tag/release
부재를 확인한 뒤 `pages-rollback.yml`의 `restore` mode로만 배포한다. Pages나 Discord
candidate가 실패하면 mixed-version public state를 남기지 않고 양쪽을 이전 상태로
복원한다. Command sync 뒤 실패하면 catalog, Oracle/image, Pages 순으로 복원하고
readback을 기록한다. Immutable tag/release는 변조하지 않으며 release 후 결함은 별도
patch release로 처리한다.

## 9. 완료 조건

- Registry blockers가 정확히 0이다.
- 38 capabilities, 4 options, 7 requirements의 current-source evidence가 있다.
- Portfolio tie만 exact lazy paging되며 정상 family에는 tie metadata가 없다.
- Score selection/tie/canonical order 어디에도 attack이 관여하지 않는다.
- GUI 세 surface와 explicit CLI path에서 모든 portfolio를 탐색할 수 있다.
- Discord는 canonical portfolio 하나만 반환하고 tie 기능을 노출하지 않는다.
- Native CLI가 Node 없이 CTK3/Fumen solution output을 생성한다.
- Image recognition/to-Fumen capability는 제거되고 upstream alias는 exclusion이다.
- Durable delegation과 exact dependency language가 지정 계약을 만족한다.
- Main SHA, Pages, Discord image/job/Oracle, command catalog, tag, release assets가
  하나의 v0.8.0 identity로 일치한다.
- Release-freeze audit, final-source revalidation, 20분 observation, rollback
  evidence가 모두 완료된다.

Unknown, partial, truncated, cancelled 결과는 complete negative, winner, total,
dependency, portfolio로 승격하지 않는다. 외부 프로젝트는 pinned comparison과
fixture 참고일 뿐 runtime dependency나 production authority가 아니다. Secret,
credential, `.env`, SSH key를 읽거나 release evidence에 기록하지 않는다.

## 10. 승인된 구현 계약

2026-08-27 read-only gap audit에서 분리한 아래 Utility, durable delegation,
`pc.minimals` WASM stack 계약은 같은 날 사용자가 전부 승인했다. 따라서 이 절은
앞 절과 동일한 규범 목표이며 별도 승인 없이 구현·검증·배포한다. 구현 중 이
계약으로도 의미가 하나로 정해지지 않는 새로운 문제가 생긴 경우에만 작업을
일시정지하고 질문한다.

### 10.1 Typed-document Utility 계약

1. Host/JSON typed envelope에는 `format=ctk3|fumen`을 필수로 둔다. Native CLI는
   `--document TEXT`와 `--document-file PATH` 중 정확히 하나를 받고, canonical
   prefix가 유일할 때만 format을 추론한다. Ambiguous input, plain grid, link file,
   16 MiB 초과, 4,096 page 초과는 거부한다. 결정적 transform에는 timeout/search/
   queue/hold/backend/worker option을 노출하지 않는다.
2. `utility.parity`는 result-bound page source로 page 하나씩 운반하고 public page
   번호는 1부터 시작한다. CLI/GUI가 전체 page를 탐색하며 Discord는 첫 page만
   bounded summary로 표시한다. 모든 page의 `feasibility_claim=false`,
   `pruning_authority=none`과 별도 `pending_garbage_occupied_cell_count`를 유지한다.
3. `utility.fumen`의 closed transform은 현재 actual codec이 증명한 `roundtrip`,
   `combine`, `split`, `get-page`, `page-shift`, `clean-comments`,
   `preserve-comments`, `to-gray`, `mirror`, `text-to-fumen`으로 한정한다. 별도
   generic operation-edit는 v0.8에 만들지 않는다. Combine은 repeated document/
   JSON array, split은 `field-document-set.v1`, get-page는 1-based, positive
   page-shift는 signed left rotation, text-to-fumen은 bounded Unicode comment
   array로 정규화한다.
4. Transform의 native text 출력은 canonical encoded document, JSON은 typed
   payload, 파일 출력은 atomic-new/no-overwrite로 한다. Web/Desktop은 copy와
   download를 모두 제공하고, Discord는 짧은 summary와 canonical attachment만
   게시한다. 원본 filename을 권위로 보존하지 않고 format 기반 안전 이름을 쓴다.
5. `utility.render`는 같은 Rust exact bitmap renderer를 WASM에도 포함한다.
   PNG는 선택한 1-based page 하나, GIF는 document page order의 timeline이며
   operation replay를 별도 frame으로 합성하지 않는다. Pending garbage는 typed
   garbage row로 그린다. Product default export limit과 Discord attachment limit을
   동시에 만족하지 못하면 typed failure다.
6. 실제 cat skin/animation authority가 없는 `sfinder-man/catimage` preset은
   임의 asset을 만들지 않고 v0.8 exclusion으로 재분류한다. 사용자가 cat skin을
   명시적으로 요구하면 별도 asset provenance와 render KAT를 먼저 확정한다.
7. `utility.render`는 target public surface가 없더라도 exact Rust renderer library
   foundation이 있으므로 `partial`은 유지하되, 현재 잘못된
   `legacy-render-file-command` evidence를 제거하고 비공개 library 근거로
   `current_surface`를 정정한다.

### 10.2 Durable delegation 계약

1. Native durable 단위는 verifier `Initialize`, 각 `Consume`, `Finish` 각각이다.
   Candidate batch만 journal하고 lifecycle의 앞뒤를 비권위로 남기지 않는다.
2. `Running`은 worker가 executable payload와 permit을 검증하고 start ACK를 보낸
   뒤의 상태다. `ResultApplied`는 actual merger가 성공한 뒤, `Completed`는 그
   durable ACK 뒤에만 기록한다.
3. v0.8 durable mode는 streaming partial 적용을 하지 않고 immutable final result를
   seal한 뒤 exactly-once application한다. Retry execution보다 at-most-once task와
   새 identity의 explicit retry를 사용한다.
4. Process restart에서 `Prepared`, `Offered`, `Accepted`, `Published`, `Running`,
   `Renewed`, `ResultSealed`, `ResultApplied` 등 모든 unresolved non-terminal phase는
   자동 resume하거나 TTL 삭제하지 않고 quarantine한다. Result bytes와 application
   transaction을 별도로 durable하게 만들기 전에는 복구 완료를 주장하지 않는다.
5. `ResultSealed` digest는 merger-ready canonical normalized final result 전체를
   결합하고 worker reply digest를 별도 transport evidence로 보존한다. 같은
   task/fence/result digest의 duplicate는 한 번만 적용하고 다른 digest는
   fail-closed한다.
6. `job_id`는 journal header에 durable하게 저장한 UUID, `task_id`는 job과 operation
   ordinal의 결정적 identity, `coordinator_id`는 source commit과 boot UUID,
   `worker_id`는 job-local numeric index로 한다. `request_sha256`은 raw command text가
   아니라 canonical typed query와 compiled problem/profile/catalog identity를 묶는다.
7. `WasmBuildProbabilityCandidateProducer`가 만든 actual canonical packet stream 하나를
   native/browser KAT 양쪽이 공유한다. Arbitrary test payload나 서로 다른 producer를
   “one candidate source” 증거로 인정하지 않는다.
8. Terminal tombstone reset은 24시간 경계 뒤 startup/explicit maintenance에서만
   compare-and-reset한다. Quarantine은 read-only inspect/archive만 허용하고 같은
   identity의 unquarantine/resume은 제공하지 않는다.
9. Native public route는 명시한 thread stack size, channel capacity/control block,
   batch/result owners를 provider-backed actual bytes로 admission한 뒤에만 연다.
   처음부터 요청된 bounded worker 수 전체를 계상하며, accounting이 없으면 serial
   성공으로 낮추거나 `REQ-V080-015`를 우회하지 않고 unavailable을 유지한다.

### 10.3 `pc.minimals` WASM stack 계약

Actual `WasmCommandRuntime::run_command_text(pc.minimals)` producer E2E는 native
debug libtest의 기본 2 MiB stack에서 overflow하고 명시적 4 MiB stack부터 통과했다.
`WasmAbiState` inline 크기는 37,456 bytes이며 governed owner 이관 재귀는 원인이
아니었다. 이 현상을 test-only stack 증설로 숨기지 않고 product runtime defect로
취급한다. 큰 parser/producer stack frame과 temporary를 fieldwise 측정해 governed
heap owner 또는 bounded store로 옮기고, 2 MiB native regression과 실제 wasm32
release worker E2E를 둘 다 통과한 뒤에만 page-owner lifecycle과 Web source
fingerprint를 acceptance한다. WASM stack 자체 증설은 high-water·artifact memory
budget·browser별 실행 증거가 별도로 생기기 전에는 사용하지 않는다.

10.1/10.2/10.3은 앞 절의 test matrix와 release gate에 합쳐 검증한다. 이 절 중
일부만 구현된 상태를 전체 public capability 또는 release 완료의 증거로 사용하지
않는다.
