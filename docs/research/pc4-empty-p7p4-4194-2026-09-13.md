# 4194 preview and empty-field P7P4 measurement boundary

## Requested case

- Source: empty field, width 10.
- Target: 4L PC (the explicit default for this measurement request).
- Supply: the original `P7P4` pattern, compiled by Clearra's existing parser.
- Product: all solutions, with the first result and full enumeration measured
  separately when the online path becomes available.
- Backend of interest: the new HF graph-backed PC4 adapter, not the old static
  beta installer, V*, policy recommendation, or Krylov.
- Keep build/download/startup time distinct from search time. Record rule
  profile, requested/actual worker count, snapshot identity, Range requests and
  bytes, cache state, emitted results, cancellation, and any fallback.

## Current source findings

At `f1a77a8230207be03fb89335fb7a913d165211b3`, the public Web route does not
instantiate the online graph adapter. The relevant source boundaries are:

- `crates/clearra-app/Cargo.toml`: `online-pc4-tablebase` is an optional feature.
- `crates/clearra-wasm/Cargo.toml` and `crates/clearra-wasm-abi/Cargo.toml`:
  the public browser build does not select that feature or export its adapter.
- `apps/clearra-web/src/workers/pc4TablebaseAssets.ts`: the GUI switch loads
  `tablebase/pc4-compact-exact-v12.bin`, the old 2,044,693-byte static beta.
- `apps/clearra-web/src/workers/clearraWorker.ts`: warmup calls
  `prewarmPc4TablebaseAssets` then `wasm.install_tablebase`. This is not an HF
  graph Range request.

Consequently, a successful old-switch run cannot be reported as a new online
TB benchmark. Neither a fallback run nor a disabled/unsupported response is a
zero-second TB success. The old beta is not enabled for this measurement.

Actual-profile qualification also remains open independently of the Web
binding: see `pc4-hf-nonempty-differential-2026-09-13.md` (13 unresolved edge
omissions) and `pc4-index-semantics-2026-09-13.md` (profile-specific index/format
binding). Synthetic-qualified tests cannot activate a production profile.

## Evidence to complete

- [x] Finish the current shared-prefix and exact-output-cap contract run.
- [x] Import and verify this source's non-publishing WASM; serve it on 4194.
- [x] Confirm the served generation and the rendered GUI with browser control.
- [ ] Enable the qualified online public adapter, then measure empty/P7P4.

The last item is a remaining implementation boundary, not a pending upstream
answer. No timing number is claimed for it in this document.

Source identity prepared for preview import:

- Commit: `f1a77a8230207be03fb89335fb7a913d165211b3`.
- Compile-input count: 2,520.
- Source SHA-256: `f82d7ca7390830491f1f7c0f83b807e2e9e879904d2ed77c3a721ac52ff5f2dd`.
- Non-publishing run: [34750402366](https://github.com/daejunnom/Clearra/actions/runs/34750402366).
- All five jobs, including the preview WASM producer, passed at that SHA.
  The ordinary Tablebase suite reports
  182 passing tests; both explicit A/B tests also pass. App PC4 reports 87,
  replay 16, native CLI process tests 17. The preview artifact is tracked
  separately from these contract outcomes.

## Completed local publication and browser check

The exact artifact from run 34750402366, attempt 1, artifact 10315249754 was
imported with the existing verified importer. It checked the five authoritative
files, their aliases, hashes and the current compile-input contract. No local
Cargo or native WASM compilation, identity restamping, or production acceptance
was involved. The managed experiment's temporary import was replaced by the
same source's frontend owner according to the one-experiment retention policy.

- WASM: `838887767ace739a76bae65cacec780fa738589167007f05adb8a23ce550b515`.
- Bindings: `e5643fc98b1c802e7579118133fcd18e194e106a3af941b1fd9e9803b4b7fe0e`.
- WASM size: 20,895,295 bytes.
- Both `/wasm/clearra_wasm.manifest.json` and `/__clearra/wasm-generation`
  served those exact identities from `http://127.0.0.1:4194/`.
- The server is loopback-only and hidden, with HMR disabled for local recovery.
- Browser verification left empty field / 4L / `P7P4` / all solutions / SRS /
  hold enabled / full supplied queue visible. It showed 11 requested/effective
  worker slots; no search was executed, so this is not measured worker activity.
- The beta TB was not enabled, and no fallback search was executed.

The first cold page exposed a separate development issue: Vite discovered
`@lucide/svelte`, `tetris-fumen` and `@tauri-apps/api/core` after initial page
loading. Its dependency-generation change coincided with a Svelte hydration
failure in the icon tree; the trace referenced two runtime chunk generations.
A reload after optimization completed restored the page. Explicitly including
those three dependencies during startup fixes the observed discovery split.
After restarting only this turn's idle preview owner with a fresh managed cache,
the first page rendered, accepted P7P4/SRS input and reported no new browser
errors. The frontend contract suite passed 9 tests. This small Vite-only change
does not alter the Rust source fingerprint or require another WASM build.

Startup warnings about the indirect generated tsconfig and recovery-mode
freshness remain visible; they are not claimed resolved. The initial misleading
same-fingerprint "stale" warning came from a missing build-identity environment
during the SvelteKit config probe; the restarted process inherited the verified
artifact's existing source/engine pins and no longer logged that mismatch.
SvelteKit's [configuration loader](https://github.com/sveltejs/kit/blob/main/packages/kit/src/core/config/index.js)
probes Vite as a build before falling back to the separate Svelte configuration.
That probe's transient config-bundle path and tsconfig-forwarder warning remain
a separate frontend build-policy follow-up; no persistent alternate build cache
was deliberately introduced.

## Measurement result

**Online HF TB empty/P7P4 search time: not measured.** The public adapter and
actual profile qualification above remain prerequisites. This is not a miss,
zero-result completion, zero-second success, or an offline performance number.
The preview publication, observed first-load fix and synthetic-prefix A/B are
complete bounded checkpoints; they do not close v0.9.0's public TB/release goal.
